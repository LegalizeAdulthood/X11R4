/* automatically generated by pepy 5.0 #1 (mss.andrew.cmu.edu), do not edit! */

#include <psap.h>

static char *pepyid = "pepy 5.0 #1 (mss.andrew.cmu.edu) of Mon May 22 20:44:32 EDT 1989";

#define	advise	PY_advise

void	advise ();

/* Generated from module ISWR9 */
# line 5 "iswr9.py"


/*
 *
 * (C) Copyright 1989 by Carnegie Mellon University
 *
 * Permission to use, copy, modify, and distribute these programs
 * and their documentation for any purpose and without fee is
 * hereby granted, provided that this copyright and permission
 * notice appear on all copies and supporting documentation, and
 * that the name of Carnegie Mellon University not be used in
 * advertising or publicity pertaining to distribution of the
 * programs without specific prior permission and notice be given
 * in supporting documentation that copying and distribution is
 * by permission of Carnegie Mellon University.
 *
 * Carnegie Mellon University makes no representations about the
 * suitability of this software for any purpose.  It is provided
 * as is, without express or implied warranty.
 *
 * Software by Ann Marks and James T. Lui,
 * Information Technology Center, Carnegie Mellon University,
 * except where noted.
 *
 */

#include <odatk.h>

#define TK_IMPL_ISWR

#include <iswr.h>

#undef TK_IMPL_ISWR

#define PEPYPARM struct isode2toolkit_t *


#ifndef PEPYPARM
#define PEPYPARM char *
#endif /* PEPYPARM */
extern PEPYPARM NullParm;

/* ARGSUSED */

int	build_ISWR9_Character__Attributes (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p0_z = NULLPE;
    register PE *p0 = &p0_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Character-Attributes: out of memory");
        return NOTOK;
    }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Character-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Character-Attributes");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_One__Of__Four__Angles (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p1 = len;

    if (((*pe) = num2prim (p1, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "One-Of-Four-Angles: out of memory");
        return NOTOK;
    }
    switch (p1) {
        case 0:	/* d0 */
            break;
        case 1:	/* d90 */
            break;
        case 2:	/* d180 */
            break;
        case 3:	/* d270 */
            break;
        default:
            advise (NULLCP, "One-Of-Four-Angles has unknown component: %d", p1);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.One-Of-Four-Angles");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_One__Of__Two__Angles (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p2 = len;

    if (((*pe) = num2prim (p2, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "One-Of-Two-Angles: out of memory");
        return NOTOK;
    }
    switch (p2) {
        case 1:	/* d90 */
            break;
        case 3:	/* d270 */
            break;
        default:
            advise (NULLCP, "One-Of-Two-Angles has unknown component: %d", p2);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.One-Of-Two-Angles");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_Alignment (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p3 = len;

    if (((*pe) = num2prim (p3, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "Alignment: out of memory");
        return NOTOK;
    }
    switch (p3) {
        case 0:	/* start-aligned */
            break;
        case 1:	/* end-aligned */
            break;
        case 2:	/* centred */
            break;
        case 3:	/* justified */
            break;
        default:
            advise (NULLCP, "Alignment has unknown component: %d", p3);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Alignment");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_Layout__Table (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p4 = NULLPE;
    PE	p5_z = NULLPE;
    register PE *p5 = &p5_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Layout-Table: out of memory");
        return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Layout-Table");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_Tabulation__Stop (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p6_z = NULLPE;
    register PE *p6 = &p6_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Tabulation-Stop: out of memory");
        return NOTOK;
    }
    (*p6) = NULLPE;

    {
        if (build_UNIV_NumericString (p6, 0, 0, NULLCP, NullParm) == NOTOK)
            return NOTOK;
        (*p6) -> pe_class = PE_CLASS_CONT;
        (*p6) -> pe_id = 0;

#ifdef DEBUG
        (void) testdebug ((*p6), "tabulation-reference");
#endif

    }
    if ((*p6) != NULLPE)
        if (set_add ((*pe), (*p6)) == NOTOK) {
            advise (NULLCP, "Tabulation-Stop bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p6) = NULLPE;

    {
        register int p7 = 0;

        if (((*p6) = num2prim (p7, PE_CLASS_CONT, 1)) == NULLPE) {
            advise (NULLCP, "tabulation-position: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p6), "tabulation-position");
#endif

    }
    if ((*p6) != NULLPE)
        if (set_add ((*pe), (*p6)) == NOTOK) {
            advise (NULLCP, "Tabulation-Stop bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p6) = NULLPE;

    {
        register int p8 = 0;

        if (((*p6) = num2prim (p8, PE_CLASS_CONT, 2)) == NULLPE) {
            advise (NULLCP, "alignment: out of memory");
            return NOTOK;
        }
        switch (p8) {
            case 0:	/* start-aligned */
                break;
            case 1:	/* end-aligned */
                break;
            case 2:	/* centred-1 */
                break;
            case 3:	/* round-aligned */
                break;
            default:
                advise (NULLCP, "alignment has unknown component: %d", p8);
                return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p6), "alignment");
#endif

    }
    if ((*p6) != NULLPE)
        if (set_add ((*pe), (*p6)) == NOTOK) {
            advise (NULLCP, "Tabulation-Stop bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p6) = NULLPE;

    if ((*p6) != NULLPE)
        if (set_add ((*pe), (*p6)) == NOTOK) {
            advise (NULLCP, "Tabulation-Stop bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Tabulation-Stop");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_Graphic__Rendition (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p9 = NULLPE;
    PE	p10_z = NULLPE;
    register PE *p10 = &p10_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Graphic-Rendition: out of memory");
        return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Graphic-Rendition");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_Character__Fonts (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p11_z = NULLPE;
    register PE *p11 = &p11_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Character-Fonts: out of memory");
        return NOTOK;
    }
    (*p11) = NULLPE;

    if ((*p11) != NULLPE)
        if (set_add ((*pe), (*p11)) == NOTOK) {
            advise (NULLCP, "Character-Fonts bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p11) = NULLPE;

    if ((*p11) != NULLPE)
        if (set_add ((*pe), (*p11)) == NOTOK) {
            advise (NULLCP, "Character-Fonts bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p11) = NULLPE;

    if ((*p11) != NULLPE)
        if (set_add ((*pe), (*p11)) == NOTOK) {
            advise (NULLCP, "Character-Fonts bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p11) = NULLPE;

    if ((*p11) != NULLPE)
        if (set_add ((*pe), (*p11)) == NOTOK) {
            advise (NULLCP, "Character-Fonts bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p11) = NULLPE;

    if ((*p11) != NULLPE)
        if (set_add ((*pe), (*p11)) == NOTOK) {
            advise (NULLCP, "Character-Fonts bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p11) = NULLPE;

    if ((*p11) != NULLPE)
        if (set_add ((*pe), (*p11)) == NOTOK) {
            advise (NULLCP, "Character-Fonts bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p11) = NULLPE;

    if ((*p11) != NULLPE)
        if (set_add ((*pe), (*p11)) == NOTOK) {
            advise (NULLCP, "Character-Fonts bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p11) = NULLPE;

    if ((*p11) != NULLPE)
        if (set_add ((*pe), (*p11)) == NOTOK) {
            advise (NULLCP, "Character-Fonts bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p11) = NULLPE;

    if ((*p11) != NULLPE)
        if (set_add ((*pe), (*p11)) == NOTOK) {
            advise (NULLCP, "Character-Fonts bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p11) = NULLPE;

    if ((*p11) != NULLPE)
        if (set_add ((*pe), (*p11)) == NOTOK) {
            advise (NULLCP, "Character-Fonts bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Character-Fonts");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_Font__Type (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p12_z = NULLPE;
    register PE *p12 = &p12_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Font-Type: out of memory");
        return NOTOK;
    }
    (*p12) = NULLPE;

    {
        register int p13 = 0;

        if (((*p12) = num2prim (p13, PE_CLASS_CONT, 0)) == NULLPE) {
            advise (NULLCP, "font-size: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p12), "font-size");
#endif

    }
    if ((*p12) != NULLPE)
        if (set_add ((*pe), (*p12)) == NOTOK) {
            advise (NULLCP, "Font-Type bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p12) = NULLPE;

    {
        register int p14 = 0;

        if (((*p12) = num2prim (p14, PE_CLASS_CONT, 1)) == NULLPE) {
            advise (NULLCP, "font-identifier: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p12), "font-identifier");
#endif

    }
    if ((*p12) != NULLPE)
        if (set_add ((*pe), (*p12)) == NOTOK) {
            advise (NULLCP, "Font-Type bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Font-Type");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_Graphic__Rendition__Aspect (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p15 = len;

    if (((*pe) = num2prim (p15, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "Graphic-Rendition-Aspect: out of memory");
        return NOTOK;
    }
    switch (p15) {
        case 0:	/* cancel */
            break;
        case 1:	/* increased-intensity */
            break;
        case 2:	/* decreased-intensity */
            break;
        case 3:	/* italicized */
            break;
        case 4:	/* underlined */
            break;
        case 5:	/* slowly-blinking */
            break;
        case 6:	/* rapidly-blinking */
            break;
        case 7:	/* negative-image */
            break;
        case 9:	/* crossed-out */
            break;
        case 10:	/* primary-font */
            break;
        case 11:	/* first-alternative-font */
            break;
        case 12:	/* second-alternative-font */
            break;
        case 13:	/* third-alternative-font */
            break;
        case 14:	/* fourth-alternative-font */
            break;
        case 15:	/* fifth-alternative-font */
            break;
        case 16:	/* sixth-alternative-font */
            break;
        case 17:	/* seventh-alternative-font */
            break;
        case 18:	/* eighth-alternative-font */
            break;
        case 19:	/* ninth-alternative-font */
            break;
        case 21:	/* doubly-underlined */
            break;
        case 22:	/* normal-intensity */
            break;
        case 23:	/* not-italicized */
            break;
        case 24:	/* not-underlined */
            break;
        case 25:	/* steady */
            break;
        case 26:	/* variable-spacing */
            break;
        case 27:	/* positive-image */
            break;
        case 29:	/* not-crossed-out */
            break;
        default:
            advise (NULLCP, "Graphic-Rendition-Aspect has unknown component: %d", p15);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Graphic-Rendition-Aspect");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_Formatting__Indicator (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p16 = len;

    if (((*pe) = num2prim (p16, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "Formatting-Indicator: out of memory");
        return NOTOK;
    }
    switch (p16) {
        case 0:	/* no */
            break;
        case 1:	/* yes */
            break;
        default:
            advise (NULLCP, "Formatting-Indicator has unknown component: %d", p16);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Formatting-Indicator");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_Itemisation (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p17_z = NULLPE;
    register PE *p17 = &p17_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Itemisation: out of memory");
        return NOTOK;
    }
    (*p17) = NULLPE;

    {
        register int p18 = 0;

        if (((*p17) = num2prim (p18, PE_CLASS_CONT, 0)) == NULLPE) {
            advise (NULLCP, "identifier-alignment: out of memory");
            return NOTOK;
        }
        switch (p18) {
            case 0:	/* no-itemisation */
                break;
            case 1:	/* start-aligned */
                break;
            case 2:	/* end-aligned */
                break;
            default:
                advise (NULLCP, "identifier-alignment has unknown component: %d", p18);
                return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p17), "identifier-alignment");
#endif

    }
    if ((*p17) != NULLPE)
        if (set_add ((*pe), (*p17)) == NOTOK) {
            advise (NULLCP, "Itemisation bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p17) = NULLPE;

    if ((*p17) != NULLPE)
        if (set_add ((*pe), (*p17)) == NOTOK) {
            advise (NULLCP, "Itemisation bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p17) = NULLPE;

    if ((*p17) != NULLPE)
        if (set_add ((*pe), (*p17)) == NOTOK) {
            advise (NULLCP, "Itemisation bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Itemisation");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_Kerning__Offset (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p19_z = NULLPE;
    register PE *p19 = &p19_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Kerning-Offset: out of memory");
        return NOTOK;
    }
    (*p19) = NULLPE;

    {
        register int p20 = 0;

        if (((*p19) = num2prim (p20, PE_CLASS_CONT, 0)) == NULLPE) {
            advise (NULLCP, "start-offset: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p19), "start-offset");
#endif

    }
    if ((*p19) != NULLPE)
        if (set_add ((*pe), (*p19)) == NOTOK) {
            advise (NULLCP, "Kerning-Offset bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p19) = NULLPE;

    {
        register int p21 = 0;

        if (((*p19) = num2prim (p21, PE_CLASS_CONT, 1)) == NULLPE) {
            advise (NULLCP, "end-offset: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p19), "end-offset");
#endif

    }
    if ((*p19) != NULLPE)
        if (set_add ((*pe), (*p19)) == NOTOK) {
            advise (NULLCP, "Kerning-Offset bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Kerning-Offset");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_Proportional__Line__Spacing (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p22 = len;

    if (((*pe) = num2prim (p22, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "Proportional-Line-Spacing: out of memory");
        return NOTOK;
    }
    switch (p22) {
        case 0:	/* no */
            break;
        case 1:	/* yes */
            break;
        default:
            advise (NULLCP, "Proportional-Line-Spacing has unknown component: %d", p22);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Proportional-Line-Spacing");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR9_Pairwise__Kerning (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p23 = len;

    if (((*pe) = num2prim (p23, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "Pairwise-Kerning: out of memory");
        return NOTOK;
    }
    switch (p23) {
        case 0:	/* no */
            break;
        case 1:	/* yes */
            break;
        default:
            advise (NULLCP, "Pairwise-Kerning has unknown component: %d", p23);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR9.Pairwise-Kerning");
#endif


    return OK;
}
