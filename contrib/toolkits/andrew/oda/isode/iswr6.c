/* automatically generated by pepy 5.0 #1 (mss.andrew.cmu.edu), do not edit! */

#include <psap.h>

static char *pepyid = "pepy 5.0 #1 (mss.andrew.cmu.edu) of Mon May 22 20:44:32 EDT 1989";

#define	advise	PY_advise

void	advise ();

/* Generated from module ISWR6 */
# line 5 "iswr6.py"


/*
 *
 * (C) Copyright 1989 by Carnegie Mellon University
 *
 * Permission to use, copy, modify, and distribute these programs
 * and their documentation for any purpose and without fee is
 * hereby granted, provided that this copyright and permission
 * notice appear on all copies and supporting documentation, and
 * that the name of Carnegie Mellon University not be used in
 * advertising or publicity pertaining to distribution of the
 * programs without specific prior permission and notice be given
 * in supporting documentation that copying and distribution is
 * by permission of Carnegie Mellon University.
 *
 * Carnegie Mellon University makes no representations about the
 * suitability of this software for any purpose.  It is provided
 * as is, without express or implied warranty.
 *
 * Software by Ann Marks and James T. Lui,
 * Information Technology Center, Carnegie Mellon University,
 * except where noted.
 *
 */

#define TK_TKI_TKERR
#define TK_TKI_APIRT

#include <odatk.h>

#undef TK_TKI_TKERR
#undef TK_TKI_APIRT

#define TK_IMPL_ISWR

#include <iswr.h>

#undef TK_IMPL_ISWR

#define PEPYPARM struct isode2toolkit_t *

INT_type dummy;


#ifndef PEPYPARM
#define PEPYPARM char *
#endif /* PEPYPARM */
extern PEPYPARM NullParm;

/* ARGSUSED */

int	build_ISWR6_Presentation__Style__Descriptor (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
struct isode2toolkit_t *  parm;
{
# line 79 "iswr6.py"
    

    STRING_type PresentStyleIDString;
    CHAR_type *PresentStyleIDChars;

    STRING_type CommentString;
    CHAR_type *CommentChars;
    INT_type CommentLength;

    STRING_type NameString;
    CHAR_type *NameChars;
    INT_type NameLength;


    PE	p0_z = NULLPE;
    register PE *p0 = &p0_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Presentation-Style-Descriptor: out of memory");
        return NOTOK;
    }
    (*p0) = NULLPE;

    {
        {
# line 98 "iswr6.py"

	    if ( GetAttr( parm->constituent,
			  at_PRESENTATION_STYLE_ID,
			  (POINTER_type) &PresentStyleIDString,
			  /* default */ BOOL_false,
			  /* mask */ (PARM_MASK_type *) 0
			) == ERROR_INT ) {
		dummy = TKERR_StartErrMsg();
		dummy = TKERR_Format("[build_ISWR6_Presentation_Style__Descriptor]: can not get attribute at_PRESENTATION_STYLE_ID" );
		dummy = TKERR_EndErrMsg();
		return ( NOTOK );
	    }
	    PresentStyleIDChars = String2Chars(PresentStyleIDString);
	    if ( PresentStyleIDChars == ERROR_CHARS ) {
		dummy = TKERR_StartErrMsg();
		dummy = TKERR_Format("[build_ISWR6_Presentation__Style__Descriptor]: can not convert string to presentation-style-identifier chars" );
		dummy = TKERR_EndErrMsg();
		return( NOTOK );
	    }
	
        }
        if (build_ISWR3_Style__Identifier (p0, 0, 0, PresentStyleIDChars , NullParm) == NOTOK)
            return NOTOK;
        (*p0) -> pe_class = PE_CLASS_APPL;
        (*p0) -> pe_id = 5;

#ifdef DEBUG
        (void) testdebug ((*p0), "style-identifier");
#endif

    }
    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Presentation-Style-Descriptor bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ( GetAttr( parm->constituent,
			at_USER_READABLE_COMMENTS,
			(POINTER_type) &CommentString,
			/* default */ BOOL_false,
			/* mask */ (PARM_MASK_type *) 0
		      ) != ERROR_INT ) {
        {
# line 123 "iswr6.py"

	    CommentLength = CommentString->length;
	    CommentChars = CommentString->sequence_value.chars;
	
        }
        if (build_ISWR4_Comment__String (p0, 0,  CommentLength , CommentChars , NullParm) == NOTOK)
            return NOTOK;
        (*p0) -> pe_class = PE_CLASS_CONT;
        (*p0) -> pe_id = 0;

#ifdef DEBUG
        (void) testdebug ((*p0), "user-readable-comments");
#endif

    }
    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Presentation-Style-Descriptor bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ( GetAttr( parm->constituent,
			at_USER_VISIBLE_NAME,
			(POINTER_type) &NameString,
			/* default */ BOOL_false,
			/* mask */ (PARM_MASK_type *) 0
		      ) != ERROR_INT ) {
        {
# line 140 "iswr6.py"

	    NameLength = NameString->length;
	    NameChars = NameString->sequence_value.chars;
	
        }
        if (build_ISWR4_Comment__String (p0, 0,  NameLength , NameChars , NullParm) == NOTOK)
            return NOTOK;
        (*p0) -> pe_class = PE_CLASS_CONT;
        (*p0) -> pe_id = 1;

#ifdef DEBUG
        (void) testdebug ((*p0), "user-visible-name");
#endif

    }
    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Presentation-Style-Descriptor bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Presentation-Style-Descriptor bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Presentation-Style-Descriptor bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Presentation-Style-Descriptor bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Presentation-Style-Descriptor bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR6.Presentation-Style-Descriptor");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR6_Presentation__Attributes (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p1_z = NULLPE;
    register PE *p1 = &p1_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Presentation-Attributes: out of memory");
        return NOTOK;
    }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (set_add ((*pe), (*p1)) == NOTOK) {
            advise (NULLCP, "Presentation-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (set_add ((*pe), (*p1)) == NOTOK) {
            advise (NULLCP, "Presentation-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (set_add ((*pe), (*p1)) == NOTOK) {
            advise (NULLCP, "Presentation-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (set_add ((*pe), (*p1)) == NOTOK) {
            advise (NULLCP, "Presentation-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (set_add ((*pe), (*p1)) == NOTOK) {
            advise (NULLCP, "Presentation-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (set_add ((*pe), (*p1)) == NOTOK) {
            advise (NULLCP, "Presentation-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR6.Presentation-Attributes");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR6_Content__Type (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p2 = len;

    if (((*pe) = num2prim (p2, PE_CLASS_APPL, 2)) == NULLPE) {
        advise (NULLCP, "Content-Type: out of memory");
        return NOTOK;
    }
    switch (p2) {
        case 0:	/* formatted-characters */
            break;
        case 1:	/* formatted-raster-graphics */
            break;
        default:
            advise (NULLCP, "Content-Type has unknown component: %d", p2);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR6.Content-Type");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR6_Content__Architecture__Class (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register OID p3;

    p3 = buffer ? str2oid (buffer) : NULLOID;
    if (p3 == NULLOID) {
        advise (NULLCP, "Content-Architecture-Class initialization fails");
        return NOTOK;
    }
    if (((*pe) = obj2prim (p3, PE_CLASS_UNIV, PE_PRIM_OID)) == NULLPE) {
        advise (NULLCP, "Content-Architecture-Class: out of memory");
        return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR6.Content-Architecture-Class");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR6_Layout__Style__Descriptor (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
struct isode2toolkit_t *  parm;
{
# line 207 "iswr6.py"
    

    STRING_type LayoutStyleIDString;
    CHAR_type *LayoutStyleIDChars;

    STRING_type CommentString;
    CHAR_type *CommentChars;
    INT_type CommentLength;

    STRING_type NameString;
    CHAR_type *NameChars;
    INT_type NameLength;


    PE	p4_z = NULLPE;
    register PE *p4 = &p4_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Layout-Style-Descriptor: out of memory");
        return NOTOK;
    }
    (*p4) = NULLPE;

    {
        {
# line 226 "iswr6.py"

	    if ( GetAttr( parm->constituent,
			  at_LAYOUT_STYLE_IDENTIFIER,
			  (POINTER_type) &LayoutStyleIDString,
			  /* default */ BOOL_false,
			  /* mask */ (PARM_MASK_type *) 0
			) == ERROR_INT ) {
		dummy = TKERR_StartErrMsg();
		dummy = TKERR_Format("[build_ISWR6_Layout_Style__Descriptor]: can not get attribute at_LAYOUT_STYLE_IDENTIFIER" );
		dummy = TKERR_EndErrMsg();
		return ( NOTOK );
	    }
	    LayoutStyleIDChars = String2Chars(LayoutStyleIDString);
	    if ( LayoutStyleIDChars == ERROR_CHARS ) {
		dummy = TKERR_StartErrMsg();
		dummy = TKERR_Format("[build_ISWR6_Layout__Style__Descriptor]: can not convert string to layout-style-identifier chars" );
		dummy = TKERR_EndErrMsg();
		return( NOTOK );
	    }
	
        }
        if (build_ISWR3_Style__Identifier (p4, 0, 0, LayoutStyleIDChars , NullParm) == NOTOK)
            return NOTOK;
        (*p4) -> pe_class = PE_CLASS_APPL;
        (*p4) -> pe_id = 5;

#ifdef DEBUG
        (void) testdebug ((*p4), "style-identifier");
#endif

    }
    if ((*p4) != NULLPE)
        if (set_add ((*pe), (*p4)) == NOTOK) {
            advise (NULLCP, "Layout-Style-Descriptor bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p4) = NULLPE;

    if ( GetAttr( parm->constituent,
			at_USER_READABLE_COMMENTS,
			(POINTER_type) &CommentString,
			/* default */ BOOL_false,
			/* mask */ (PARM_MASK_type *) 0
		      ) != ERROR_INT ) {
        {
# line 251 "iswr6.py"

	    CommentLength = CommentString->length;
	    CommentChars = CommentString->sequence_value.chars;
	
        }
        if (build_ISWR4_Comment__String (p4, 0,  CommentLength , CommentChars , NullParm) == NOTOK)
            return NOTOK;
        (*p4) -> pe_class = PE_CLASS_CONT;
        (*p4) -> pe_id = 0;

#ifdef DEBUG
        (void) testdebug ((*p4), "user-readable-comments");
#endif

    }
    if ((*p4) != NULLPE)
        if (set_add ((*pe), (*p4)) == NOTOK) {
            advise (NULLCP, "Layout-Style-Descriptor bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p4) = NULLPE;

    if ( GetAttr( parm->constituent,
			at_USER_VISIBLE_NAME,
			(POINTER_type) &NameString,
			/* default */ BOOL_false,
			/* mask */ (PARM_MASK_type *) 0
		      ) != ERROR_INT ) {
        {
# line 268 "iswr6.py"

	    NameLength = NameString->length;
	    NameChars = NameString->sequence_value.chars;
	
        }
        if (build_ISWR4_Comment__String (p4, 0,  NameLength , NameChars , NullParm) == NOTOK)
            return NOTOK;
        (*p4) -> pe_class = PE_CLASS_CONT;
        (*p4) -> pe_id = 1;

#ifdef DEBUG
        (void) testdebug ((*p4), "user-visible-name");
#endif

    }
    if ((*p4) != NULLPE)
        if (set_add ((*pe), (*p4)) == NOTOK) {
            advise (NULLCP, "Layout-Style-Descriptor bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p4) = NULLPE;

    if ((*p4) != NULLPE)
        if (set_add ((*pe), (*p4)) == NOTOK) {
            advise (NULLCP, "Layout-Style-Descriptor bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR6.Layout-Style-Descriptor");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR6_Layout__Directives (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p5_z = NULLPE;
    register PE *p5 = &p5_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Layout-Directives: out of memory");
        return NOTOK;
    }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Layout-Directives bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Layout-Directives bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Layout-Directives bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Layout-Directives bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Layout-Directives bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Layout-Directives bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Layout-Directives bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Layout-Directives bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Layout-Directives bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Layout-Directives bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Layout-Directives bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR6.Layout-Directives");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR6_Separation (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p6_z = NULLPE;
    register PE *p6 = &p6_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Separation: out of memory");
        return NOTOK;
    }
    (*p6) = NULLPE;

    if ((*p6) != NULLPE)
        if (set_add ((*pe), (*p6)) == NOTOK) {
            advise (NULLCP, "Separation bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p6) = NULLPE;

    if ((*p6) != NULLPE)
        if (set_add ((*pe), (*p6)) == NOTOK) {
            advise (NULLCP, "Separation bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p6) = NULLPE;

    if ((*p6) != NULLPE)
        if (set_add ((*pe), (*p6)) == NOTOK) {
            advise (NULLCP, "Separation bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR6.Separation");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR6_Offset (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p7_z = NULLPE;
    register PE *p7 = &p7_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Offset: out of memory");
        return NOTOK;
    }
    (*p7) = NULLPE;

    if ((*p7) != NULLPE)
        if (set_add ((*pe), (*p7)) == NOTOK) {
            advise (NULLCP, "Offset bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p7) = NULLPE;

    if ((*p7) != NULLPE)
        if (set_add ((*pe), (*p7)) == NOTOK) {
            advise (NULLCP, "Offset bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p7) = NULLPE;

    if ((*p7) != NULLPE)
        if (set_add ((*pe), (*p7)) == NOTOK) {
            advise (NULLCP, "Offset bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p7) = NULLPE;

    if ((*p7) != NULLPE)
        if (set_add ((*pe), (*p7)) == NOTOK) {
            advise (NULLCP, "Offset bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR6.Offset");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR6_Fill__Order (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p8 = len;

    if (((*pe) = num2prim (p8, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "Fill-Order: out of memory");
        return NOTOK;
    }
    switch (p8) {
        case 0:	/* normal */
            break;
        case 1:	/* reverse */
            break;
        default:
            advise (NULLCP, "Fill-Order has unknown component: %d", p8);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR6.Fill-Order");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR6_Concatenation (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p9 = len;

    if (((*pe) = num2prim (p9, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "Concatenation: out of memory");
        return NOTOK;
    }
    switch (p9) {
        case 0:	/* non-concatenated */
            break;
        case 1:	/* concatenated */
            break;
        default:
            advise (NULLCP, "Concatenation has unknown component: %d", p9);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR6.Concatenation");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR6_Same__Layout__Object (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p10_z = NULLPE;
    register PE *p10 = &p10_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Same-Layout-Object: out of memory");
        return NOTOK;
    }
    (*p10) = NULLPE;

    {

#ifdef DEBUG
        (void) testdebug ((*p10), "to-logical-object");
#endif

    }
    if ((*p10) != NULLPE)
        if (set_add ((*pe), (*p10)) == NOTOK) {
            advise (NULLCP, "Same-Layout-Object bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p10) = NULLPE;

    if ((*p10) != NULLPE)
        if (set_add ((*pe), (*p10)) == NOTOK) {
            advise (NULLCP, "Same-Layout-Object bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR6.Same-Layout-Object");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR6_Block__Alignment (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p11 = len;

    if (((*pe) = num2prim (p11, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "Block-Alignment: out of memory");
        return NOTOK;
    }
    switch (p11) {
        case 0:	/* right-hand */
            break;
        case 1:	/* left-hand */
            break;
        case 2:	/* centred */
            break;
        case 3:	/* null */
            break;
        default:
            advise (NULLCP, "Block-Alignment has unknown component: %d", p11);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR6.Block-Alignment");
#endif


    return OK;
}
