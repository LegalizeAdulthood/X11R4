/* automatically generated by pepy 5.0 #1 (mss.andrew.cmu.edu), do not edit! */

#include <psap.h>

static char *pepyid = "pepy 5.0 #1 (mss.andrew.cmu.edu) of Mon May 22 20:44:32 EDT 1989";

#define	advise	PY_advise

void	advise ();

/* Generated from module ISWR18 */
# line 5 "iswr18.py"


/*
 *
 * (C) Copyright 1989 by Carnegie Mellon University
 *
 * Permission to use, copy, modify, and distribute these programs
 * and their documentation for any purpose and without fee is
 * hereby granted, provided that this copyright and permission
 * notice appear on all copies and supporting documentation, and
 * that the name of Carnegie Mellon University not be used in
 * advertising or publicity pertaining to distribution of the
 * programs without specific prior permission and notice be given
 * in supporting documentation that copying and distribution is
 * by permission of Carnegie Mellon University.
 *
 * Carnegie Mellon University makes no representations about the
 * suitability of this software for any purpose.  It is provided
 * as is, without express or implied warranty.
 *
 * Software by Ann Marks and James T. Lui,
 * Information Technology Center, Carnegie Mellon University,
 * except where noted.
 *
 */

#include <odatk.h>

#define TK_IMPL_ISRD

#include <isrd.h>

#undef TK_IMPL_ISRD

#define PEPYPARM struct isode2toolkit_t *


#ifndef PEPYPARM
#define PEPYPARM char *
#endif /* PEPYPARM */
extern PEPYPARM NullParm;

/* ARGSUSED */

int	build_ISWR18_Font__Reference (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
struct isode2toolkit_t *  parm;
{
    PE	p0_z = NULLPE;
    register PE *p0 = &p0_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Font-Reference: out of memory");
        return NOTOK;
    }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (seq_add ((*pe), (*p0), -1) == NOTOK) {
            advise (NULLCP, "Font-Reference bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Font-Reference");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Character__Complement (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p1_z = NULLPE;
    register PE *p1 = &p1_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Character-Complement: out of memory");
        return NOTOK;
    }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (seq_add ((*pe), (*p1), -1) == NOTOK) {
            advise (NULLCP, "Character-Complement bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (seq_add ((*pe), (*p1), -1) == NOTOK) {
            advise (NULLCP, "Character-Complement bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (seq_add ((*pe), (*p1), -1) == NOTOK) {
            advise (NULLCP, "Character-Complement bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (seq_add ((*pe), (*p1), -1) == NOTOK) {
            advise (NULLCP, "Character-Complement bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (seq_add ((*pe), (*p1), -1) == NOTOK) {
            advise (NULLCP, "Character-Complement bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Character-Complement");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Ligature__Table (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p2_z = NULLPE;
    register PE *p2 = &p2_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Ligature-Table: out of memory");
        return NOTOK;
    }
    (*p2) = NULLPE;

    if ((*p2) != NULLPE)
        if (seq_add ((*pe), (*p2), -1) == NOTOK) {
            advise (NULLCP, "Ligature-Table bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p2) = NULLPE;

    {	/* ligatures */
        PE	p3 = NULLPE;
        PE	p4_z = NULLPE;
        register PE *p4 = &p4_z;

        if (((*p2) = pe_alloc (PE_CLASS_CONT, PE_FORM_CONS, 1)) == NULLPE) {
            advise (NULLCP, "ligatures: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p2), "ligatures");
#endif

    }

    if ((*p2) != NULLPE)
        if (seq_add ((*pe), (*p2), -1) == NOTOK) {
            advise (NULLCP, "Ligature-Table bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Ligature-Table");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Ligature (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p5_z = NULLPE;
    register PE *p5 = &p5_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Ligature: out of memory");
        return NOTOK;
    }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (seq_add ((*pe), (*p5), -1) == NOTOK) {
            advise (NULLCP, "Ligature bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    {	/* ligature-character */
        if (build_ISWR18_Structured__Name (p5, 0, 0, NULLCP, NullParm) == NOTOK)
            return NOTOK;
        (*p5) -> pe_class = PE_CLASS_CONT;
        (*p5) -> pe_id = 1;

#ifdef DEBUG
        (void) testdebug ((*p5), "ligature-character");
#endif

    }

    if ((*p5) != NULLPE)
        if (seq_add ((*pe), (*p5), -1) == NOTOK) {
            advise (NULLCP, "Ligature bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    {	/* equivalent-characters */
        PE	p6 = NULLPE;
        PE	p7_z = NULLPE;
        register PE *p7 = &p7_z;

        if (((*p5) = pe_alloc (PE_CLASS_CONT, PE_FORM_CONS, 2)) == NULLPE) {
            advise (NULLCP, "equivalent-characters: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p5), "equivalent-characters");
#endif

    }

    if ((*p5) != NULLPE)
        if (seq_add ((*pe), (*p5), -1) == NOTOK) {
            advise (NULLCP, "Ligature bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Ligature");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Writing__Modes (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p8_z = NULLPE;
    register PE *p8 = &p8_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Writing-Modes: out of memory");
        return NOTOK;
    }
    (*p8) = NULLPE;

    if ((*p8) != NULLPE)
        if (seq_add ((*pe), (*p8), -1) == NOTOK) {
            advise (NULLCP, "Writing-Modes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p8) = NULLPE;

    if ((*p8) != NULLPE)
        if (seq_add ((*pe), (*p8), -1) == NOTOK) {
            advise (NULLCP, "Writing-Modes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p8) = NULLPE;

    if ((*p8) != NULLPE)
        if (seq_add ((*pe), (*p8), -1) == NOTOK) {
            advise (NULLCP, "Writing-Modes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p8) = NULLPE;

    if ((*p8) != NULLPE)
        if (seq_add ((*pe), (*p8), -1) == NOTOK) {
            advise (NULLCP, "Writing-Modes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p8) = NULLPE;

    if ((*p8) != NULLPE)
        if (seq_add ((*pe), (*p8), -1) == NOTOK) {
            advise (NULLCP, "Writing-Modes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Writing-Modes");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Modal__Font__Attributes (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p9_z = NULLPE;
    register PE *p9 = &p9_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Modal-Font-Attributes: out of memory");
        return NOTOK;
    }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p9) = NULLPE;

    if ((*p9) != NULLPE)
        if (seq_add ((*pe), (*p9), -1) == NOTOK) {
            advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Modal-Font-Attributes");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Space__Adjustment__Tracks (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p10_z = NULLPE;
    register PE *p10 = &p10_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Space-Adjustment-Tracks: out of memory");
        return NOTOK;
    }
    (*p10) = NULLPE;

    if ((*p10) != NULLPE)
        if (seq_add ((*pe), (*p10), -1) == NOTOK) {
            advise (NULLCP, "Space-Adjustment-Tracks bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p10) = NULLPE;

    if ((*p10) != NULLPE)
        if (seq_add ((*pe), (*p10), -1) == NOTOK) {
            advise (NULLCP, "Space-Adjustment-Tracks bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p10) = NULLPE;

    if ((*p10) != NULLPE)
        if (seq_add ((*pe), (*p10), -1) == NOTOK) {
            advise (NULLCP, "Space-Adjustment-Tracks bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p10) = NULLPE;

    if ((*p10) != NULLPE)
        if (seq_add ((*pe), (*p10), -1) == NOTOK) {
            advise (NULLCP, "Space-Adjustment-Tracks bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p10) = NULLPE;

    if ((*p10) != NULLPE)
        if (seq_add ((*pe), (*p10), -1) == NOTOK) {
            advise (NULLCP, "Space-Adjustment-Tracks bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Space-Adjustment-Tracks");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Track (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p11_z = NULLPE;
    register PE *p11 = &p11_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Track: out of memory");
        return NOTOK;
    }
    (*p11) = NULLPE;

    if ((*p11) != NULLPE)
        if (seq_add ((*pe), (*p11), -1) == NOTOK) {
            advise (NULLCP, "Track bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p11) = NULLPE;

    if ((*p11) != NULLPE)
        if (seq_add ((*pe), (*p11), -1) == NOTOK) {
            advise (NULLCP, "Track bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Track");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Scores (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p12_z = NULLPE;
    register PE *p12 = &p12_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Scores: out of memory");
        return NOTOK;
    }
    (*p12) = NULLPE;

    if ((*p12) != NULLPE)
        if (seq_add ((*pe), (*p12), -1) == NOTOK) {
            advise (NULLCP, "Scores bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p12) = NULLPE;

    if ((*p12) != NULLPE)
        if (seq_add ((*pe), (*p12), -1) == NOTOK) {
            advise (NULLCP, "Scores bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p12) = NULLPE;

    if ((*p12) != NULLPE)
        if (seq_add ((*pe), (*p12), -1) == NOTOK) {
            advise (NULLCP, "Scores bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p12) = NULLPE;

    if ((*p12) != NULLPE)
        if (seq_add ((*pe), (*p12), -1) == NOTOK) {
            advise (NULLCP, "Scores bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p12) = NULLPE;

    if ((*p12) != NULLPE)
        if (seq_add ((*pe), (*p12), -1) == NOTOK) {
            advise (NULLCP, "Scores bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Scores");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Score (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p13_z = NULLPE;
    register PE *p13 = &p13_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Score: out of memory");
        return NOTOK;
    }
    (*p13) = NULLPE;

    if ((*p13) != NULLPE)
        if (seq_add ((*pe), (*p13), -1) == NOTOK) {
            advise (NULLCP, "Score bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p13) = NULLPE;

    if ((*p13) != NULLPE)
        if (seq_add ((*pe), (*p13), -1) == NOTOK) {
            advise (NULLCP, "Score bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Score");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Variant__Scripts (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p14_z = NULLPE;
    register PE *p14 = &p14_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Variant-Scripts: out of memory");
        return NOTOK;
    }
    (*p14) = NULLPE;

    if ((*p14) != NULLPE)
        if (seq_add ((*pe), (*p14), -1) == NOTOK) {
            advise (NULLCP, "Variant-Scripts bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p14) = NULLPE;

    if ((*p14) != NULLPE)
        if (seq_add ((*pe), (*p14), -1) == NOTOK) {
            advise (NULLCP, "Variant-Scripts bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p14) = NULLPE;

    if ((*p14) != NULLPE)
        if (seq_add ((*pe), (*p14), -1) == NOTOK) {
            advise (NULLCP, "Variant-Scripts bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p14) = NULLPE;

    if ((*p14) != NULLPE)
        if (seq_add ((*pe), (*p14), -1) == NOTOK) {
            advise (NULLCP, "Variant-Scripts bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Variant-Scripts");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Variant__Script (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p15_z = NULLPE;
    register PE *p15 = &p15_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Variant-Script: out of memory");
        return NOTOK;
    }
    (*p15) = NULLPE;

    if ((*p15) != NULLPE)
        if (seq_add ((*pe), (*p15), -1) == NOTOK) {
            advise (NULLCP, "Variant-Script bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p15) = NULLPE;

    if ((*p15) != NULLPE)
        if (seq_add ((*pe), (*p15), -1) == NOTOK) {
            advise (NULLCP, "Variant-Script bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p15) = NULLPE;

    if ((*p15) != NULLPE)
        if (seq_add ((*pe), (*p15), -1) == NOTOK) {
            advise (NULLCP, "Variant-Script bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p15) = NULLPE;

    if ((*p15) != NULLPE)
        if (seq_add ((*pe), (*p15), -1) == NOTOK) {
            advise (NULLCP, "Variant-Script bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Variant-Script");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Modal__Character__Attributes (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p16_z = NULLPE;
    register PE *p16 = &p16_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Modal-Character-Attributes: out of memory");
        return NOTOK;
    }
    (*p16) = NULLPE;

    if ((*p16) != NULLPE)
        if (seq_add ((*pe), (*p16), -1) == NOTOK) {
            advise (NULLCP, "Modal-Character-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p16) = NULLPE;

    {	/* characters */
        PE	p17 = NULLPE;
        PE	p18_z = NULLPE;
        register PE *p18 = &p18_z;

        if (((*p16) = pe_alloc (PE_CLASS_CONT, PE_FORM_CONS, 1)) == NULLPE) {
            advise (NULLCP, "characters: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p16), "characters");
#endif

    }

    if ((*p16) != NULLPE)
        if (seq_add ((*pe), (*p16), -1) == NOTOK) {
            advise (NULLCP, "Modal-Character-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Modal-Character-Attributes");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Char__Pair__Esc__Adjusts (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p19_z = NULLPE;
    register PE *p19 = &p19_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Char-Pair-Esc-Adjusts: out of memory");
        return NOTOK;
    }
    (*p19) = NULLPE;

    if ((*p19) != NULLPE)
        if (seq_add ((*pe), (*p19), -1) == NOTOK) {
            advise (NULLCP, "Char-Pair-Esc-Adjusts bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p19) = NULLPE;

    {	/* successor-characters */
        PE	p20 = NULLPE;
        PE	p21_z = NULLPE;
        register PE *p21 = &p21_z;

        if (((*p19) = pe_alloc (PE_CLASS_CONT, PE_FORM_CONS, 1)) == NULLPE) {
            advise (NULLCP, "successor-characters: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p19), "successor-characters");
#endif

    }

    if ((*p19) != NULLPE)
        if (seq_add ((*pe), (*p19), -1) == NOTOK) {
            advise (NULLCP, "Char-Pair-Esc-Adjusts bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Char-Pair-Esc-Adjusts");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Character__Shape__Technologies (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p22_z = NULLPE;
    register PE *p22 = &p22_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Character-Shape-Technologies: out of memory");
        return NOTOK;
    }
    (*p22) = NULLPE;

    if ((*p22) != NULLPE)
        if (seq_add ((*pe), (*p22), -1) == NOTOK) {
            advise (NULLCP, "Character-Shape-Technologies bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p22) = NULLPE;

    if ((*p22) != NULLPE)
        if (seq_add ((*pe), (*p22), -1) == NOTOK) {
            advise (NULLCP, "Character-Shape-Technologies bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p22) = NULLPE;

    if ((*p22) != NULLPE)
        if (seq_add ((*pe), (*p22), -1) == NOTOK) {
            advise (NULLCP, "Character-Shape-Technologies bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p22) = NULLPE;

    if ((*p22) != NULLPE)
        if (seq_add ((*pe), (*p22), -1) == NOTOK) {
            advise (NULLCP, "Character-Shape-Technologies bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p22) = NULLPE;

    if ((*p22) != NULLPE)
        if (seq_add ((*pe), (*p22), -1) == NOTOK) {
            advise (NULLCP, "Character-Shape-Technologies bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p22) = NULLPE;

    if ((*p22) != NULLPE)
        if (seq_add ((*pe), (*p22), -1) == NOTOK) {
            advise (NULLCP, "Character-Shape-Technologies bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p22) = NULLPE;

    if ((*p22) != NULLPE)
        if (seq_add ((*pe), (*p22), -1) == NOTOK) {
            advise (NULLCP, "Character-Shape-Technologies bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p22) = NULLPE;

    if ((*p22) != NULLPE)
        if (seq_add ((*pe), (*p22), -1) == NOTOK) {
            advise (NULLCP, "Character-Shape-Technologies bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Character-Shape-Technologies");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Technology__Attributes (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p23_z = NULLPE;
    register PE *p23 = &p23_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Technology-Attributes: out of memory");
        return NOTOK;
    }
    (*p23) = NULLPE;

    if ((*p23) != NULLPE)
        if (seq_add ((*pe), (*p23), -1) == NOTOK) {
            advise (NULLCP, "Technology-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p23) = NULLPE;

    if ((*p23) != NULLPE)
        if (seq_add ((*pe), (*p23), -1) == NOTOK) {
            advise (NULLCP, "Technology-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p23) = NULLPE;

    if ((*p23) != NULLPE)
        if (seq_add ((*pe), (*p23), -1) == NOTOK) {
            advise (NULLCP, "Technology-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p23) = NULLPE;

    if ((*p23) != NULLPE)
        if (seq_add ((*pe), (*p23), -1) == NOTOK) {
            advise (NULLCP, "Technology-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p23) = NULLPE;

    if ((*p23) != NULLPE)
        if (seq_add ((*pe), (*p23), -1) == NOTOK) {
            advise (NULLCP, "Technology-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p23) = NULLPE;

    if ((*p23) != NULLPE)
        if (seq_add ((*pe), (*p23), -1) == NOTOK) {
            advise (NULLCP, "Technology-Attributes bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Technology-Attributes");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Non__ISO__Attribute (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p24_z = NULLPE;
    register PE *p24 = &p24_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Non-ISO-Attribute: out of memory");
        return NOTOK;
    }
    (*p24) = NULLPE;

    {	/* attribute-name */
        if (build_ISWR18_Structured__Name (p24, 0, 0, NULLCP, NullParm) == NOTOK)
            return NOTOK;
        (*p24) -> pe_class = PE_CLASS_CONT;
        (*p24) -> pe_id = 0;

#ifdef DEBUG
        (void) testdebug ((*p24), "attribute-name");
#endif

    }

    if ((*p24) != NULLPE)
        if (seq_add ((*pe), (*p24), -1) == NOTOK) {
            advise (NULLCP, "Non-ISO-Attribute bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p24) = NULLPE;

    {	/* attribute-value */

#ifdef DEBUG
        (void) testdebug ((*p24), "attribute-value");
#endif

    }

    if ((*p24) != NULLPE)
        if (seq_add ((*pe), (*p24), -1) == NOTOK) {
            advise (NULLCP, "Non-ISO-Attribute bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Non-ISO-Attribute");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Structured__Name (pe, explicit, len, buffer, parm)
PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Structured-Name");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Default__Name__Prefix (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p25 = NULLPE;
    PE	p26_z = NULLPE;
    register PE *p26 = &p26_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Default-Name-Prefix: out of memory");
        return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Default-Name-Prefix");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Identifier (pe, explicit, len, buffer, parm)
PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Identifier");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR18_Real (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p27_z = NULLPE;
    register PE *p27 = &p27_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Real: out of memory");
        return NOTOK;
    }
    (*p27) = NULLPE;

    {	/* numerator */
        register int p28 = 0;

        if (((*p27) = num2prim (p28, PE_CLASS_CONT, 0)) == NULLPE) {
            advise (NULLCP, "numerator: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p27), "numerator");
#endif

    }

    if ((*p27) != NULLPE)
        if (seq_add ((*pe), (*p27), -1) == NOTOK) {
            advise (NULLCP, "Real bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p27) = NULLPE;

    if ((*p27) != NULLPE)
        if (seq_add ((*pe), (*p27), -1) == NOTOK) {
            advise (NULLCP, "Real bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR18.Real");
#endif


    return OK;
}
