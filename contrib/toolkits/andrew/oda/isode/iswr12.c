/* automatically generated by pepy 5.0 #1 (mss.andrew.cmu.edu), do not edit! */

#include <psap.h>

static char *pepyid = "pepy 5.0 #1 (mss.andrew.cmu.edu) of Mon May 22 20:44:32 EDT 1989";

#define	advise	PY_advise

void	advise ();

/* Generated from module ISWR12 */
# line 5 "iswr12.py"


/*
 *
 * (C) Copyright 1989 by Carnegie Mellon University
 *
 * Permission to use, copy, modify, and distribute these programs
 * and their documentation for any purpose and without fee is
 * hereby granted, provided that this copyright and permission
 * notice appear on all copies and supporting documentation, and
 * that the name of Carnegie Mellon University not be used in
 * advertising or publicity pertaining to distribution of the
 * programs without specific prior permission and notice be given
 * in supporting documentation that copying and distribution is
 * by permission of Carnegie Mellon University.
 *
 * Carnegie Mellon University makes no representations about the
 * suitability of this software for any purpose.  It is provided
 * as is, without express or implied warranty.
 *
 * Software by Ann Marks and James T. Lui,
 * Information Technology Center, Carnegie Mellon University,
 * except where noted.
 *
 */

#include <odatk.h>

#define TK_IMPL_ISWR

#include <iswr.h>

#undef TK_IMPL_ISWR

#define PEPYPARM struct isode2toolkit_t *


#ifndef PEPYPARM
#define PEPYPARM char *
#endif /* PEPYPARM */
extern PEPYPARM NullParm;

/* ARGSUSED */

int	build_ISWR12_Raster__Graphics__Attributes (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p0_z = NULLPE;
    register PE *p0 = &p0_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Raster-Graphics-Attributes: out of memory");
        return NOTOK;
    }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Raster-Graphics-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Raster-Graphics-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Raster-Graphics-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Raster-Graphics-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Raster-Graphics-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Raster-Graphics-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Raster-Graphics-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Raster-Graphics-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR12.Raster-Graphics-Attributes");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR12_One__Of__Four__Angles (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p1 = len;

    if (((*pe) = num2prim (p1, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "One-Of-Four-Angles: out of memory");
        return NOTOK;
    }
    switch (p1) {
        case 0:	/* d0 */
            break;
        case 1:	/* d90 */
            break;
        case 2:	/* d180 */
            break;
        case 3:	/* d270 */
            break;
        default:
            advise (NULLCP, "One-Of-Four-Angles has unknown component: %d", p1);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR12.One-Of-Four-Angles");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR12_One__Of__Two__Angles (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p2 = len;

    if (((*pe) = num2prim (p2, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "One-Of-Two-Angles: out of memory");
        return NOTOK;
    }
    switch (p2) {
        case 1:	/* d90 */
            break;
        case 3:	/* d270 */
            break;
        default:
            advise (NULLCP, "One-Of-Two-Angles has unknown component: %d", p2);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR12.One-Of-Two-Angles");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR12_Pel__Transmission__Density (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    register int p3 = len;

    if (((*pe) = num2prim (p3, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
        advise (NULLCP, "Pel-Transmission-Density: out of memory");
        return NOTOK;
    }
    switch (p3) {
        case 1:	/* p6 */
            break;
        case 2:	/* p5 */
            break;
        case 3:	/* p4 */
            break;
        case 4:	/* p3 */
            break;
        case 5:	/* p2 */
            break;
        case 6:	/* p1 */
            break;
        default:
            advise (NULLCP, "Pel-Transmission-Density has unknown component: %d", p3);
            return NOTOK;
    }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR12.Pel-Transmission-Density");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR12_Clipping (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p4_z = NULLPE;
    register PE *p4 = &p4_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Clipping: out of memory");
        return NOTOK;
    }
    (*p4) = NULLPE;

    if ((*p4) != NULLPE)
        if (seq_add ((*pe), (*p4), -1) == NOTOK) {
            advise (NULLCP, "Clipping bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p4) = NULLPE;

    {	/* second-coordinate-pair */
        if (build_ISWR12_Coordinate__Pair (p4, 0, 0, NULLCP, NullParm) == NOTOK)
            return NOTOK;
        (*p4) -> pe_class = PE_CLASS_CONT;
        (*p4) -> pe_id = 1;

#ifdef DEBUG
        (void) testdebug ((*p4), "second-coordinate-pair");
#endif

    }

    if ((*p4) != NULLPE)
        if (seq_add ((*pe), (*p4), -1) == NOTOK) {
            advise (NULLCP, "Clipping bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR12.Clipping");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR12_Coordinate__Pair (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p5_z = NULLPE;
    register PE *p5 = &p5_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Coordinate-Pair: out of memory");
        return NOTOK;
    }
    (*p5) = NULLPE;

    {	/* x-coordinate */
        register int p6 = 0;

        if (((*p5) = num2prim (p6, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
            advise (NULLCP, "x-coordinate: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p5), "x-coordinate");
#endif

    }

    if ((*p5) != NULLPE)
        if (seq_add ((*pe), (*p5), -1) == NOTOK) {
            advise (NULLCP, "Coordinate-Pair bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    {	/* y-coordinate */
        register int p7 = 0;

        if (((*p5) = num2prim (p7, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
            advise (NULLCP, "y-coordinate: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p5), "y-coordinate");
#endif

    }

    if ((*p5) != NULLPE)
        if (seq_add ((*pe), (*p5), -1) == NOTOK) {
            advise (NULLCP, "Coordinate-Pair bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR12.Coordinate-Pair");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR12_Pel__Spacing (pe, explicit, len, buffer, parm)
PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR12.Pel-Spacing");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR12_Spacing__Ratio (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p8_z = NULLPE;
    register PE *p8 = &p8_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SEQ)) == NULLPE) {
        advise (NULLCP, "Spacing-Ratio: out of memory");
        return NOTOK;
    }
    (*p8) = NULLPE;

    {	/* line-spacing-value */
        register int p9 = 0;

        if (((*p8) = num2prim (p9, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
            advise (NULLCP, "line-spacing-value: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p8), "line-spacing-value");
#endif

    }

    if ((*p8) != NULLPE)
        if (seq_add ((*pe), (*p8), -1) == NOTOK) {
            advise (NULLCP, "Spacing-Ratio bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p8) = NULLPE;

    {	/* pel-spacing-value */
        register int p10 = 0;

        if (((*p8) = num2prim (p10, PE_CLASS_UNIV, PE_PRIM_INT)) == NULLPE) {
            advise (NULLCP, "pel-spacing-value: out of memory");
            return NOTOK;
        }

#ifdef DEBUG
        (void) testdebug ((*p8), "pel-spacing-value");
#endif

    }

    if ((*p8) != NULLPE)
        if (seq_add ((*pe), (*p8), -1) == NOTOK) {
            advise (NULLCP, "Spacing-Ratio bad sequence: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR12.Spacing-Ratio");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR12_Image__Dimensions (pe, explicit, len, buffer, parm)
PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR12.Image-Dimensions");
#endif


    return OK;
}
