/* automatically generated by pepy 5.0 #1 (mss.andrew.cmu.edu), do not edit! */

#include <psap.h>

static char *pepyid = "pepy 5.0 #1 (mss.andrew.cmu.edu) of Mon May 22 20:44:32 EDT 1989";

#define	advise	PY_advise

void	advise ();

/* Generated from module ISWR7 */
# line 5 "iswr7.py"


/*
 *
 * (C) Copyright 1989 by Carnegie Mellon University
 *
 * Permission to use, copy, modify, and distribute these programs
 * and their documentation for any purpose and without fee is
 * hereby granted, provided that this copyright and permission
 * notice appear on all copies and supporting documentation, and
 * that the name of Carnegie Mellon University not be used in
 * advertising or publicity pertaining to distribution of the
 * programs without specific prior permission and notice be given
 * in supporting documentation that copying and distribution is
 * by permission of Carnegie Mellon University.
 *
 * Carnegie Mellon University makes no representations about the
 * suitability of this software for any purpose.  It is provided
 * as is, without express or implied warranty.
 *
 * Software by Ann Marks and James T. Lui,
 * Information Technology Center, Carnegie Mellon University,
 * except where noted.
 *
 */

#include <odatk.h>

#define TK_IMPL_ISWR

#include <iswr.h>

#undef TK_IMPL_ISWR

#define PEPYPARM struct isode2toolkit_t *


#ifndef PEPYPARM
#define PEPYPARM char *
#endif /* PEPYPARM */
extern PEPYPARM NullParm;

/* ARGSUSED */

int	build_ISWR7_Default__Value__Lists__Layout (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p0_z = NULLPE;
    register PE *p0 = &p0_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Default-Value-Lists-Layout: out of memory");
        return NOTOK;
    }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Default-Value-Lists-Layout bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Default-Value-Lists-Layout bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p0) = NULLPE;

    if ((*p0) != NULLPE)
        if (set_add ((*pe), (*p0)) == NOTOK) {
            advise (NULLCP, "Default-Value-Lists-Layout bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR7.Default-Value-Lists-Layout");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR7_Default__Value__Lists__Logical (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p1_z = NULLPE;
    register PE *p1 = &p1_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Default-Value-Lists-Logical: out of memory");
        return NOTOK;
    }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (set_add ((*pe), (*p1)) == NOTOK) {
            advise (NULLCP, "Default-Value-Lists-Logical bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p1) = NULLPE;

    if ((*p1) != NULLPE)
        if (set_add ((*pe), (*p1)) == NOTOK) {
            advise (NULLCP, "Default-Value-Lists-Logical bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR7.Default-Value-Lists-Logical");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR7_Page__Attributes (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p2_z = NULLPE;
    register PE *p2 = &p2_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Page-Attributes: out of memory");
        return NOTOK;
    }
    (*p2) = NULLPE;

    if ((*p2) != NULLPE)
        if (set_add ((*pe), (*p2)) == NOTOK) {
            advise (NULLCP, "Page-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p2) = NULLPE;

    if ((*p2) != NULLPE)
        if (set_add ((*pe), (*p2)) == NOTOK) {
            advise (NULLCP, "Page-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p2) = NULLPE;

    if ((*p2) != NULLPE)
        if (set_add ((*pe), (*p2)) == NOTOK) {
            advise (NULLCP, "Page-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p2) = NULLPE;

    if ((*p2) != NULLPE)
        if (set_add ((*pe), (*p2)) == NOTOK) {
            advise (NULLCP, "Page-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p2) = NULLPE;

    if ((*p2) != NULLPE)
        if (set_add ((*pe), (*p2)) == NOTOK) {
            advise (NULLCP, "Page-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p2) = NULLPE;

    if ((*p2) != NULLPE)
        if (set_add ((*pe), (*p2)) == NOTOK) {
            advise (NULLCP, "Page-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p2) = NULLPE;

    if ((*p2) != NULLPE)
        if (set_add ((*pe), (*p2)) == NOTOK) {
            advise (NULLCP, "Page-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p2) = NULLPE;

    if ((*p2) != NULLPE)
        if (set_add ((*pe), (*p2)) == NOTOK) {
            advise (NULLCP, "Page-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR7.Page-Attributes");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR7_Frame__Attributes (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p3_z = NULLPE;
    register PE *p3 = &p3_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Frame-Attributes: out of memory");
        return NOTOK;
    }
    (*p3) = NULLPE;

    if ((*p3) != NULLPE)
        if (set_add ((*pe), (*p3)) == NOTOK) {
            advise (NULLCP, "Frame-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p3) = NULLPE;

    if ((*p3) != NULLPE)
        if (set_add ((*pe), (*p3)) == NOTOK) {
            advise (NULLCP, "Frame-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p3) = NULLPE;

    if ((*p3) != NULLPE)
        if (set_add ((*pe), (*p3)) == NOTOK) {
            advise (NULLCP, "Frame-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p3) = NULLPE;

    if ((*p3) != NULLPE)
        if (set_add ((*pe), (*p3)) == NOTOK) {
            advise (NULLCP, "Frame-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p3) = NULLPE;

    if ((*p3) != NULLPE)
        if (set_add ((*pe), (*p3)) == NOTOK) {
            advise (NULLCP, "Frame-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p3) = NULLPE;

    if ((*p3) != NULLPE)
        if (set_add ((*pe), (*p3)) == NOTOK) {
            advise (NULLCP, "Frame-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p3) = NULLPE;

    if ((*p3) != NULLPE)
        if (set_add ((*pe), (*p3)) == NOTOK) {
            advise (NULLCP, "Frame-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR7.Frame-Attributes");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR7_Block__Attributes (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p4_z = NULLPE;
    register PE *p4 = &p4_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Block-Attributes: out of memory");
        return NOTOK;
    }
    (*p4) = NULLPE;

    if ((*p4) != NULLPE)
        if (set_add ((*pe), (*p4)) == NOTOK) {
            advise (NULLCP, "Block-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p4) = NULLPE;

    if ((*p4) != NULLPE)
        if (set_add ((*pe), (*p4)) == NOTOK) {
            advise (NULLCP, "Block-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p4) = NULLPE;

    if ((*p4) != NULLPE)
        if (set_add ((*pe), (*p4)) == NOTOK) {
            advise (NULLCP, "Block-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p4) = NULLPE;

    if ((*p4) != NULLPE)
        if (set_add ((*pe), (*p4)) == NOTOK) {
            advise (NULLCP, "Block-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p4) = NULLPE;

    if ((*p4) != NULLPE)
        if (set_add ((*pe), (*p4)) == NOTOK) {
            advise (NULLCP, "Block-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p4) = NULLPE;

    if ((*p4) != NULLPE)
        if (set_add ((*pe), (*p4)) == NOTOK) {
            advise (NULLCP, "Block-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p4) = NULLPE;

    if ((*p4) != NULLPE)
        if (set_add ((*pe), (*p4)) == NOTOK) {
            advise (NULLCP, "Block-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR7.Block-Attributes");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR7_Composite__Logical__Attributes (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p5_z = NULLPE;
    register PE *p5 = &p5_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Composite-Logical-Attributes: out of memory");
        return NOTOK;
    }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Composite-Logical-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p5) = NULLPE;

    if ((*p5) != NULLPE)
        if (set_add ((*pe), (*p5)) == NOTOK) {
            advise (NULLCP, "Composite-Logical-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR7.Composite-Logical-Attributes");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR7_Basic__Logical__Attributes (pe, explicit, len, buffer, parm)
register PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{
    PE	p6_z = NULLPE;
    register PE *p6 = &p6_z;

    if (((*pe) = pe_alloc (PE_CLASS_UNIV, PE_FORM_CONS, PE_CONS_SET)) == NULLPE) {
        advise (NULLCP, "Basic-Logical-Attributes: out of memory");
        return NOTOK;
    }
    (*p6) = NULLPE;

    if ((*p6) != NULLPE)
        if (set_add ((*pe), (*p6)) == NOTOK) {
            advise (NULLCP, "Basic-Logical-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p6) = NULLPE;

    if ((*p6) != NULLPE)
        if (set_add ((*pe), (*p6)) == NOTOK) {
            advise (NULLCP, "Basic-Logical-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p6) = NULLPE;

    if ((*p6) != NULLPE)
        if (set_add ((*pe), (*p6)) == NOTOK) {
            advise (NULLCP, "Basic-Logical-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }
    (*p6) = NULLPE;

    if ((*p6) != NULLPE)
        if (set_add ((*pe), (*p6)) == NOTOK) {
            advise (NULLCP, "Basic-Logical-Attributes bad set: %s",
                    pe_error ((*pe) -> pe_errno));
            return NOTOK;
        }

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR7.Basic-Logical-Attributes");
#endif


    return OK;
}

/* ARGSUSED */

int	build_ISWR7_Attribute (pe, explicit, len, buffer, parm)
PE     *pe;
int	explicit;
int	len;
char   *buffer;
PEPYPARM parm;
{

#ifdef DEBUG
    (void) testdebug ((*pe), "ISWR7.Attribute");
#endif


    return OK;
}
