/* automatically generated by pepy 5.0 #1 (mss.andrew.cmu.edu), do not edit! */

#include <psap.h>

static char *pepyid = "pepy 5.0 #1 (mss.andrew.cmu.edu) of Mon May 22 20:44:32 EDT 1989";

#define	advise	PY_advise

void	advise ();

/* Generated from module ISPR18 */
# line 5 "ispr18.py"


/*
 *
 * (C) Copyright 1989 by Carnegie Mellon University
 *
 * Permission to use, copy, modify, and distribute these programs
 * and their documentation for any purpose and without fee is
 * hereby granted, provided that this copyright and permission
 * notice appear on all copies and supporting documentation, and
 * that the name of Carnegie Mellon University not be used in
 * advertising or publicity pertaining to distribution of the
 * programs without specific prior permission and notice be given
 * in supporting documentation that copying and distribution is
 * by permission of Carnegie Mellon University.
 *
 * Carnegie Mellon University makes no representations about the
 * suitability of this software for any purpose.  It is provided
 * as is, without express or implied warranty.
 *
 * Software by Ann Marks and James T. Lui,
 * Information Technology Center, Carnegie Mellon University,
 * except where noted.
 *
 */


#ifndef PEPYPARM
#define PEPYPARM char *
#endif /* PEPYPARM */
extern PEPYPARM NullParm;

/* ARGSUSED */

int	print_ISPR18_Font__Reference (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p0;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Font-Reference bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Font-Reference bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p0 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Font-Reference bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p0;

    vpush ();
    {
        register PE p1;

        if ((p1 = first_member (pe)) != NULLPE
                && PE_ID (p1 -> pe_class, p1 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p1 = NULLPE;
        if (p1 != NULLPE) {
            p0 = p1;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p1, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p2;

        if ((p2 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p2 -> pe_class, p2 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 1))
            p2 = NULLPE;
        if (p2 != NULLPE) {
            p0 = p2;

            {	/* font-name */
                vname ("font-name");
                if (print_ISPR18_Structured__Name (p2, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p3;

        if ((p3 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p3 -> pe_class, p3 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 2))
            p3 = NULLPE;
        if (p3 != NULLPE) {
            p0 = p3;

            {	/* device-independent-font-name */
                vname ("device-independent-font-name");
                if (print_ISPR18_Structured__Name (p3, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p4;

        if ((p4 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p4 -> pe_class, p4 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 3))
            p4 = NULLPE;
        if (p4 != NULLPE) {
            p0 = p4;

            {	/* proprietary-data-indicator */
                register int p5;

                vname ("proprietary-data-indicator");
                if ((p5 = prim2num (p4)) == NOTOK
                        && p4 -> pe_errno != PE_ERR_NONE) {
                    advise (NULLCP, "proprietary-data-indicator bad integer: %s",
                            pe_error (p4 -> pe_errno));
                    return NOTOK;
                }
                vprint ("%d", p5);
            }
        }
    }

    {
        register PE p6;

        if ((p6 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p6 -> pe_class, p6 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 4))
            p6 = NULLPE;
        if (p6 != NULLPE) {
            p0 = p6;

            {	/* typeface-name */
                vname ("typeface-name");
                if (print_ISPR18_Structured__Name (p6, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p7;

        if ((p7 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p7 -> pe_class, p7 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 5))
            p7 = NULLPE;
        if (p7 != NULLPE) {
            p0 = p7;

            {	/* family-name */
                vname ("family-name");
                if (print_ISPR18_Structured__Name (p7, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p8;

        if ((p8 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p8 -> pe_class, p8 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 6))
            p8 = NULLPE;
        if (p8 != NULLPE) {
            p0 = p8;

            {	/* posture */
                register int p9;

                vname ("posture");
                if ((p9 = prim2num (p8)) == NOTOK
                        && p8 -> pe_errno != PE_ERR_NONE) {
                    advise (NULLCP, "posture bad integer: %s",
                            pe_error (p8 -> pe_errno));
                    return NOTOK;
                }
                vprint ("%d", p9);
            }
        }
    }

    {
        register PE p10;

        if ((p10 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p10 -> pe_class, p10 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 7))
            p10 = NULLPE;
        if (p10 != NULLPE) {
            p0 = p10;

            {	/* weight */
                register int p11;

                vname ("weight");
                if ((p11 = prim2num (p10)) == NOTOK
                        && p10 -> pe_errno != PE_ERR_NONE) {
                    advise (NULLCP, "weight bad integer: %s",
                            pe_error (p10 -> pe_errno));
                    return NOTOK;
                }
                vprint ("%d", p11);
            }
        }
    }

    {
        register PE p12;

        if ((p12 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p12 -> pe_class, p12 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 8))
            p12 = NULLPE;
        if (p12 != NULLPE) {
            p0 = p12;

            {	/* proportionate-width */
                register int p13;

                vname ("proportionate-width");
                if ((p13 = prim2num (p12)) == NOTOK
                        && p12 -> pe_errno != PE_ERR_NONE) {
                    advise (NULLCP, "proportionate-width bad integer: %s",
                            pe_error (p12 -> pe_errno));
                    return NOTOK;
                }
                vprint ("%d", p13);
            }
        }
    }

    {
        register PE p14;

        if ((p14 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p14 -> pe_class, p14 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 9))
            p14 = NULLPE;
        if (p14 != NULLPE) {
            p0 = p14;

            {	/* character-complement */
                vname ("character-complement");
                if (print_ISPR18_Character__Complement (p14, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p15;

        if ((p15 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p15 -> pe_class, p15 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 10))
            p15 = NULLPE;
        if (p15 != NULLPE) {
            p0 = p15;

            {	/* number-of-characters */
                register int p16;

                vname ("number-of-characters");
                if ((p16 = prim2num (p15)) == NOTOK
                        && p15 -> pe_errno != PE_ERR_NONE) {
                    advise (NULLCP, "number-of-characters bad integer: %s",
                            pe_error (p15 -> pe_errno));
                    return NOTOK;
                }
                vprint ("%d", p16);
            }
        }
    }

    {
        register PE p17;

        if ((p17 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p17 -> pe_class, p17 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 11))
            p17 = NULLPE;
        if (p17 != NULLPE) {
            p0 = p17;

            {	/* ligature-table */
                vname ("ligature-table");
                if (print_ISPR18_Ligature__Table (p17, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p18;

        if ((p18 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p18 -> pe_class, p18 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 12))
            p18 = NULLPE;
        if (p18 != NULLPE) {
            p0 = p18;

            {	/* design-size */
                vname ("design-size");
                if (print_ISPR18_Real (p18, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p19;

        if ((p19 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p19 -> pe_class, p19 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 13))
            p19 = NULLPE;
        if (p19 != NULLPE) {
            p0 = p19;

            {	/* minimum-size */
                vname ("minimum-size");
                if (print_ISPR18_Real (p19, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p20;

        if ((p20 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p20 -> pe_class, p20 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 14))
            p20 = NULLPE;
        if (p20 != NULLPE) {
            p0 = p20;

            {	/* maximum-size */
                vname ("maximum-size");
                if (print_ISPR18_Real (p20, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p21;

        if ((p21 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p21 -> pe_class, p21 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 15))
            p21 = NULLPE;
        if (p21 != NULLPE) {
            p0 = p21;

            {	/* design-classification */
                register PE p22;

                vname ("design-classification");
                if ((p22 = prim2seq (p21)) == NULLPE) {
                    advise (NULLCP, "design-classification bad sequence: %s",
                            pe_error (p21 -> pe_errno));
                    return NOTOK;
                }
                p21 = p22;

                vpush ();
                for (p22 = first_member (p21); p22; p22 = next_member (p21, p22)) {
                    register int p23;

                    if (p22 -> pe_class != PE_CLASS_UNIV
                            || p22 -> pe_form != PE_FORM_PRIM
                            || p22 -> pe_id != PE_PRIM_INT) {
                        advise (NULLCP, "element bad class/form/id: %s/%d/0x%x",
                                pe_classlist[p22 -> pe_class], p22 -> pe_form, p22 -> pe_id);
                        return NOTOK;
                    }

                    if ((p23 = prim2num (p22)) == NOTOK
                            && p22 -> pe_errno != PE_ERR_NONE) {
                        advise (NULLCP, "element bad integer: %s",
                                pe_error (p22 -> pe_errno));
                        return NOTOK;
                    }
                    vprint ("%d", p23);
                }
                vpop ();
            }
        }
    }

    {
        register PE p24;

        if ((p24 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p24 -> pe_class, p24 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 16))
            p24 = NULLPE;
        if (p24 != NULLPE) {
            p0 = p24;

            {	/* structure */
                register int p25;

                vname ("structure");
                if ((p25 = prim2num (p24)) == NOTOK
                        && p24 -> pe_errno != PE_ERR_NONE) {
                    advise (NULLCP, "structure bad integer: %s",
                            pe_error (p24 -> pe_errno));
                    return NOTOK;
                }
                vprint ("%d", p25);
            }
        }
    }

    {
        register PE p26;

        if ((p26 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p26 -> pe_class, p26 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 17))
            p26 = NULLPE;
        if (p26 != NULLPE) {
            p0 = p26;

            {	/* minimum-feature-size */
                vname ("minimum-feature-size");
                if (print_ISPR18_Real (p26, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p27;

        if ((p27 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p27 -> pe_class, p27 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 18))
            p27 = NULLPE;
        if (p27 != NULLPE) {
            p0 = p27;

            {	/* writing-modes */
                vname ("writing-modes");
                if (print_ISPR18_Writing__Modes (p27, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p28;

        if ((p28 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p28 -> pe_class, p28 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 19))
            p28 = NULLPE;
        if (p28 != NULLPE) {
            p0 = p28;

            {	/* writing-mode */
                vname ("writing-mode");
                if (print_ISPR18_Structured__Name (p28, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p29;

        if ((p29 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p29 -> pe_class, p29 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 20))
            p29 = NULLPE;
        if (p29 != NULLPE) {
            p0 = p29;

            {	/* character-shape-technologies */
                vname ("character-shape-technologies");
                if (print_ISPR18_Character__Shape__Technologies (p29, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p30;

        if ((p30 = (pe != p0 ? next_member (pe, p0) : first_member (pe))) != NULLPE) {
            p0 = p30;

            {	/* non-ISO-attributes */
                register PE p31;

                vname ("non-ISO-attributes");
                if ((p31 = prim2set (p30)) == NULLPE) {
                    advise (NULLCP, "non-ISO-attributes bad set: %s",
                            pe_error (p30 -> pe_errno));
                    return NOTOK;
                }
                p30 = p31;

                vpush ();
                for (p31 = first_member (p30); p31; p31 = next_member (p30, p31)) {
                    if (print_ISPR18_Non__ISO__Attribute (p31, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                        return NOTOK;
                }
                vpop ();
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 22) {
        advise (NULLCP, "Font-Reference has too many elements(22): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Character__Complement (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p32;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Character-Complement bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Character-Complement bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p32 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Character-Complement bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p32;

    vpush ();
    {
        register PE p33;

        if ((p33 = first_member (pe)) != NULLPE
                && PE_ID (p33 -> pe_class, p33 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p33 = NULLPE;
        if (p33 != NULLPE) {
            p32 = p33;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p33, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p34;

        if ((p34 = (pe != p32 ? next_member (pe, p32) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p34 -> pe_class, p34 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 1))
            p34 = NULLPE;
        if (p34 != NULLPE) {
            p32 = p34;

            {	/* included-character-collections */
                register PE p35;

                vname ("included-character-collections");
                if ((p35 = prim2set (p34)) == NULLPE) {
                    advise (NULLCP, "included-character-collections bad set: %s",
                            pe_error (p34 -> pe_errno));
                    return NOTOK;
                }
                p34 = p35;

                vpush ();
                for (p35 = first_member (p34); p35; p35 = next_member (p34, p35)) {
                    if (print_ISPR18_Structured__Name (p35, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                        return NOTOK;
                }
                vpop ();
            }
        }
    }

    {
        register PE p36;

        if ((p36 = (pe != p32 ? next_member (pe, p32) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p36 -> pe_class, p36 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 2))
            p36 = NULLPE;
        if (p36 != NULLPE) {
            p32 = p36;

            {	/* excluded-character-collections */
                register PE p37;

                vname ("excluded-character-collections");
                if ((p37 = prim2set (p36)) == NULLPE) {
                    advise (NULLCP, "excluded-character-collections bad set: %s",
                            pe_error (p36 -> pe_errno));
                    return NOTOK;
                }
                p36 = p37;

                vpush ();
                for (p37 = first_member (p36); p37; p37 = next_member (p36, p37)) {
                    if (print_ISPR18_Structured__Name (p37, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                        return NOTOK;
                }
                vpop ();
            }
        }
    }

    {
        register PE p38;

        if ((p38 = (pe != p32 ? next_member (pe, p32) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p38 -> pe_class, p38 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 3))
            p38 = NULLPE;
        if (p38 != NULLPE) {
            p32 = p38;

            {	/* included-characters */
                register PE p39;

                vname ("included-characters");
                if ((p39 = prim2set (p38)) == NULLPE) {
                    advise (NULLCP, "included-characters bad set: %s",
                            pe_error (p38 -> pe_errno));
                    return NOTOK;
                }
                p38 = p39;

                vpush ();
                for (p39 = first_member (p38); p39; p39 = next_member (p38, p39)) {
                    if (print_ISPR18_Structured__Name (p39, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                        return NOTOK;
                }
                vpop ();
            }
        }
    }

    {
        register PE p40;

        if ((p40 = (pe != p32 ? next_member (pe, p32) : first_member (pe))) != NULLPE) {
            p32 = p40;

            {	/* excluded-characters */
                register PE p41;

                vname ("excluded-characters");
                if ((p41 = prim2set (p40)) == NULLPE) {
                    advise (NULLCP, "excluded-characters bad set: %s",
                            pe_error (p40 -> pe_errno));
                    return NOTOK;
                }
                p40 = p41;

                vpush ();
                for (p41 = first_member (p40); p41; p41 = next_member (p40, p41)) {
                    if (print_ISPR18_Structured__Name (p41, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                        return NOTOK;
                }
                vpop ();
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 5) {
        advise (NULLCP, "Character-Complement has too many elements(5): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Ligature__Table (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p42;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Ligature-Table bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Ligature-Table bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p42 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Ligature-Table bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p42;

    vpush ();
    {
        register PE p43;

        if ((p43 = first_member (pe)) != NULLPE
                && PE_ID (p43 -> pe_class, p43 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p43 = NULLPE;
        if (p43 != NULLPE) {
            p42 = p43;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p43, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p44;

        if ((p44 = (pe != p42 ? next_member (pe, p42) : first_member (pe))) != NULLPE) {
            p42 = p44;

            {	/* ligatures */
                register PE p45;

                vname ("ligatures");
                if ((p45 = prim2set (p44)) == NULLPE) {
                    advise (NULLCP, "ligatures bad set: %s",
                            pe_error (p44 -> pe_errno));
                    return NOTOK;
                }
                p44 = p45;

                vpush ();
                for (p45 = first_member (p44); p45; p45 = next_member (p44, p45)) {
                    if (print_ISPR18_Ligature (p45, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                        return NOTOK;
                }
                vpop ();
            }
        }
        else {
            advise (NULLCP, "Ligature-Table missing ligatures element");
            return NOTOK;
        }

    }

    vpop ();

    if (pe -> pe_cardinal > 2) {
        advise (NULLCP, "Ligature-Table has too many elements(2): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Ligature (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p46;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Ligature bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Ligature bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p46 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Ligature bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p46;

    vpush ();
    {
        register PE p47;

        if ((p47 = first_member (pe)) != NULLPE
                && PE_ID (p47 -> pe_class, p47 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p47 = NULLPE;
        if (p47 != NULLPE) {
            p46 = p47;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p47, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p48;

        if ((p48 = (pe != p46 ? next_member (pe, p46) : first_member (pe))) != NULLPE) {
            p46 = p48;

            {	/* ligature-character */
                vname ("ligature-character");
                if (print_ISPR18_Structured__Name (p48, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
        else {
            advise (NULLCP, "Ligature missing ligature-character element");
            return NOTOK;
        }

    }

    {
        register PE p49;

        if ((p49 = (pe != p46 ? next_member (pe, p46) : first_member (pe))) != NULLPE) {
            p46 = p49;

            {	/* equivalent-characters */
                register PE p50;

                vname ("equivalent-characters");
                if ((p50 = prim2seq (p49)) == NULLPE) {
                    advise (NULLCP, "equivalent-characters bad sequence: %s",
                            pe_error (p49 -> pe_errno));
                    return NOTOK;
                }
                p49 = p50;

                vpush ();
                for (p50 = first_member (p49); p50; p50 = next_member (p49, p50)) {
                    if (print_ISPR18_Structured__Name (p50, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                        return NOTOK;
                }
                vpop ();
            }
        }
        else {
            advise (NULLCP, "Ligature missing equivalent-characters element");
            return NOTOK;
        }

    }

    vpop ();

    if (pe -> pe_cardinal > 3) {
        advise (NULLCP, "Ligature has too many elements(3): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Writing__Modes (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p51;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Writing-Modes bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Writing-Modes bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p51 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Writing-Modes bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p51;

    vpush ();
    {
        register PE p52;

        if ((p52 = first_member (pe)) != NULLPE
                && PE_ID (p52 -> pe_class, p52 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p52 = NULLPE;
        if (p52 != NULLPE) {
            p51 = p52;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p52, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p53;

        if ((p53 = (pe != p51 ? next_member (pe, p51) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p53 -> pe_class, p53 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 1))
            p53 = NULLPE;
        if (p53 != NULLPE) {
            p51 = p53;

            {	/* iso-0 */
                vname ("iso-0");
                if (print_ISPR18_Modal__Font__Attributes (p53, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p54;

        if ((p54 = (pe != p51 ? next_member (pe, p51) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p54 -> pe_class, p54 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 2))
            p54 = NULLPE;
        if (p54 != NULLPE) {
            p51 = p54;

            {	/* iso-180 */
                vname ("iso-180");
                if (print_ISPR18_Modal__Font__Attributes (p54, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p55;

        if ((p55 = (pe != p51 ? next_member (pe, p51) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p55 -> pe_class, p55 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 3))
            p55 = NULLPE;
        if (p55 != NULLPE) {
            p51 = p55;

            {	/* iso-270 */
                vname ("iso-270");
                if (print_ISPR18_Modal__Font__Attributes (p55, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p56;

        if ((p56 = (pe != p51 ? next_member (pe, p51) : first_member (pe))) != NULLPE) {
            p51 = p56;

            {	/* non-ISO-modes */
                register PE p57;

                vname ("non-ISO-modes");
                if ((p57 = prim2set (p56)) == NULLPE) {
                    advise (NULLCP, "non-ISO-modes bad set: %s",
                            pe_error (p56 -> pe_errno));
                    return NOTOK;
                }
                p56 = p57;

                vpush ();
                for (p57 = first_member (p56); p57; p57 = next_member (p56, p57)) {
                    register PE p58;

                    if (p57 -> pe_class != PE_CLASS_UNIV
                            || p57 -> pe_form != PE_FORM_CONS
                            || p57 -> pe_id != PE_CONS_SEQ) {
                        advise (NULLCP, "member bad class/form/id: %s/%d/0x%x",
                                pe_classlist[p57 -> pe_class], p57 -> pe_form, p57 -> pe_id);
                        return NOTOK;
                    }

                    if ((p58 = prim2seq (p57)) == NULLPE) {
                        advise (NULLCP, "member bad sequence: %s",
                                pe_error (p57 -> pe_errno));
                        return NOTOK;
                    }
                    p57 = p58;

                    vpush ();
                    {
                        register PE p59;

                        if ((p59 = first_member (p57)) != NULLPE) {
                            p58 = p59;

                            {	/* mode-name */
                                vname ("mode-name");
                                if (print_ISPR18_Structured__Name (p59, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                        else {
                            advise (NULLCP, "member missing mode-name element");
                            return NOTOK;
                        }

                    }

                    {
                        register PE p60;

                        if ((p60 = (p57 != p58 ? next_member (p57, p58) : first_member (p57))) != NULLPE) {
                            p58 = p60;

                            {	/* mode-attributes */
                                vname ("mode-attributes");
                                if (print_ISPR18_Modal__Font__Attributes (p60, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                        else {
                            advise (NULLCP, "member missing mode-attributes element");
                            return NOTOK;
                        }

                    }

                    vpop ();

                    if (p57 -> pe_cardinal > 2) {
                        advise (NULLCP, "member has too many elements(2): %d",
                                p57 -> pe_cardinal);
                        return NOTOK;
                    }
                }
                vpop ();
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 5) {
        advise (NULLCP, "Writing-Modes has too many elements(5): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Modal__Font__Attributes (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p61;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Modal-Font-Attributes bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Modal-Font-Attributes bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p61 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Modal-Font-Attributes bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p61;

    vpush ();
    {
        register PE p62;

        if ((p62 = first_member (pe)) != NULLPE
                && PE_ID (p62 -> pe_class, p62 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p62 = NULLPE;
        if (p62 != NULLPE) {
            p61 = p62;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p62, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p63;

        if ((p63 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p63 -> pe_class, p63 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 1))
            p63 = NULLPE;
        if (p63 != NULLPE) {
            p61 = p63;

            {	/* escapement-direction */
                vname ("escapement-direction");
                if (print_ISPR18_Real (p63, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p64;

        if ((p64 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p64 -> pe_class, p64 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 2))
            p64 = NULLPE;
        if (p64 != NULLPE) {
            p61 = p64;

            {	/* escapement-class */
                register int p65;

                vname ("escapement-class");
                if ((p65 = prim2num (p64)) == NOTOK
                        && p64 -> pe_errno != PE_ERR_NONE) {
                    advise (NULLCP, "escapement-class bad integer: %s",
                            pe_error (p64 -> pe_errno));
                    return NOTOK;
                }
                vprint ("%d", p65);
            }
        }
    }

    {
        register PE p66;

        if ((p66 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p66 -> pe_class, p66 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 3))
            p66 = NULLPE;
        if (p66 != NULLPE) {
            p61 = p66;

            {	/* average-escapement */
                vname ("average-escapement");
                if (print_ISPR18_Real (p66, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p67;

        if ((p67 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p67 -> pe_class, p67 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 4))
            p67 = NULLPE;
        if (p67 != NULLPE) {
            p61 = p67;

            {	/* average-lower-case-escapement */
                vname ("average-lower-case-escapement");
                if (print_ISPR18_Real (p67, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p68;

        if ((p68 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p68 -> pe_class, p68 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 5))
            p68 = NULLPE;
        if (p68 != NULLPE) {
            p61 = p68;

            {	/* average-capital-escapement */
                vname ("average-capital-escapement");
                if (print_ISPR18_Real (p68, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p69;

        if ((p69 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p69 -> pe_class, p69 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 6))
            p69 = NULLPE;
        if (p69 != NULLPE) {
            p61 = p69;

            {	/* weighted-average-escapement */
                vname ("weighted-average-escapement");
                if (print_ISPR18_Real (p69, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p70;

        if ((p70 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p70 -> pe_class, p70 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 7))
            p70 = NULLPE;
        if (p70 != NULLPE) {
            p61 = p70;

            {	/* tabular-escapement */
                register PE p71;

                vname ("tabular-escapement");
                if ((p71 = prim2seq (p70)) == NULLPE) {
                    advise (NULLCP, "tabular-escapement bad sequence: %s",
                            pe_error (p70 -> pe_errno));
                    return NOTOK;
                }
                p70 = p71;

                vpush ();
                {
                    register PE p72;

                    if ((p72 = first_member (p70)) != NULLPE
                            && PE_ID (p72 -> pe_class, p72 -> pe_id)
                                    != PE_ID (PE_CLASS_CONT, 0))
                        p72 = NULLPE;
                    if (p72 != NULLPE) {
                        p71 = p72;

                        {	/* x-component */
                            vname ("x-component");
                            if (print_ISPR18_Real (p72, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                return NOTOK;
                        }
                    }
                }

                {
                    register PE p73;

                    if ((p73 = (p70 != p71 ? next_member (p70, p71) : first_member (p70))) != NULLPE) {
                        p71 = p73;

                        {	/* y-component */
                            vname ("y-component");
                            if (print_ISPR18_Real (p73, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                return NOTOK;
                        }
                    }
                }

                vpop ();

                if (p70 -> pe_cardinal > 2) {
                    advise (NULLCP, "tabular-escapement has too many elements(2): %d",
                            p70 -> pe_cardinal);
                    return NOTOK;
                }
            }
        }
    }

    {
        register PE p74;

        if ((p74 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p74 -> pe_class, p74 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 8))
            p74 = NULLPE;
        if (p74 != NULLPE) {
            p61 = p74;

            {	/* maximum-forward-extent */
                vname ("maximum-forward-extent");
                if (print_ISPR18_Real (p74, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p75;

        if ((p75 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p75 -> pe_class, p75 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 9))
            p75 = NULLPE;
        if (p75 != NULLPE) {
            p61 = p75;

            {	/* maximum-left-extent */
                vname ("maximum-left-extent");
                if (print_ISPR18_Real (p75, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p76;

        if ((p76 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p76 -> pe_class, p76 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 10))
            p76 = NULLPE;
        if (p76 != NULLPE) {
            p61 = p76;

            {	/* maximum-backward-extent */
                vname ("maximum-backward-extent");
                if (print_ISPR18_Real (p76, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p77;

        if ((p77 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p77 -> pe_class, p77 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 11))
            p77 = NULLPE;
        if (p77 != NULLPE) {
            p61 = p77;

            {	/* maximum-right-extent */
                vname ("maximum-right-extent");
                if (print_ISPR18_Real (p77, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p78;

        if ((p78 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p78 -> pe_class, p78 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 12))
            p78 = NULLPE;
        if (p78 != NULLPE) {
            p61 = p78;

            {	/* class-pairwise-esc-adjustments */
                register PE p79;

                vname ("class-pairwise-esc-adjustments");
                if ((p79 = prim2set (p78)) == NULLPE) {
                    advise (NULLCP, "class-pairwise-esc-adjustments bad set: %s",
                            pe_error (p78 -> pe_errno));
                    return NOTOK;
                }
                p78 = p79;

                vpush ();
                for (p79 = first_member (p78); p79; p79 = next_member (p78, p79)) {
                    register PE p80;

                    if (p79 -> pe_class != PE_CLASS_UNIV
                            || p79 -> pe_form != PE_FORM_CONS
                            || p79 -> pe_id != PE_CONS_SEQ) {
                        advise (NULLCP, "member bad class/form/id: %s/%d/0x%x",
                                pe_classlist[p79 -> pe_class], p79 -> pe_form, p79 -> pe_id);
                        return NOTOK;
                    }

                    if ((p80 = prim2seq (p79)) == NULLPE) {
                        advise (NULLCP, "member bad sequence: %s",
                                pe_error (p79 -> pe_errno));
                        return NOTOK;
                    }
                    p79 = p80;

                    vpush ();
                    {
                        register PE p81;

                        if ((p81 = first_member (p79)) != NULLPE) {
                            p80 = p81;

                            {	/* first-character-forward-class */
                                register int p82;

                                vname ("first-character-forward-class");
                                if ((p82 = prim2num (p81)) == NOTOK
                                        && p81 -> pe_errno != PE_ERR_NONE) {
                                    advise (NULLCP, "first-character-forward-class bad integer: %s",
                                            pe_error (p81 -> pe_errno));
                                    return NOTOK;
                                }
                                vprint ("%d", p82);
                            }
                        }
                        else {
                            advise (NULLCP, "member missing first-character-forward-class element");
                            return NOTOK;
                        }

                    }

                    {
                        register PE p83;

                        if ((p83 = (p79 != p80 ? next_member (p79, p80) : first_member (p79))) != NULLPE) {
                            p80 = p83;

                            {	/* second-character-backward-class */
                                register int p84;

                                vname ("second-character-backward-class");
                                if ((p84 = prim2num (p83)) == NOTOK
                                        && p83 -> pe_errno != PE_ERR_NONE) {
                                    advise (NULLCP, "second-character-backward-class bad integer: %s",
                                            pe_error (p83 -> pe_errno));
                                    return NOTOK;
                                }
                                vprint ("%d", p84);
                            }
                        }
                        else {
                            advise (NULLCP, "member missing second-character-backward-class element");
                            return NOTOK;
                        }

                    }

                    {
                        register PE p85;

                        if ((p85 = (p79 != p80 ? next_member (p79, p80) : first_member (p79))) 
                                    != NULLPE
                                && PE_ID (p85 -> pe_class, p85 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 2))
                            p85 = NULLPE;
                        if (p85 != NULLPE) {
                            p80 = p85;

                            {	/* x-adjustment */
                                vname ("x-adjustment");
                                if (print_ISPR18_Real (p85, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p86;

                        if ((p86 = (p79 != p80 ? next_member (p79, p80) : first_member (p79))) != NULLPE) {
                            p80 = p86;

                            {	/* y-adjustment */
                                vname ("y-adjustment");
                                if (print_ISPR18_Real (p86, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    vpop ();

                    if (p79 -> pe_cardinal > 4) {
                        advise (NULLCP, "member has too many elements(4): %d",
                                p79 -> pe_cardinal);
                        return NOTOK;
                    }
                }
                vpop ();
            }
        }
    }

    {
        register PE p87;

        if ((p87 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p87 -> pe_class, p87 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 13))
            p87 = NULLPE;
        if (p87 != NULLPE) {
            p61 = p87;

            {	/* minimum-space-amplification */
                vname ("minimum-space-amplification");
                if (print_ISPR18_Real (p87, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p88;

        if ((p88 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p88 -> pe_class, p88 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 14))
            p88 = NULLPE;
        if (p88 != NULLPE) {
            p61 = p88;

            {	/* maximum-space-amplification */
                vname ("maximum-space-amplification");
                if (print_ISPR18_Real (p88, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p89;

        if ((p89 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p89 -> pe_class, p89 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 15))
            p89 = NULLPE;
        if (p89 != NULLPE) {
            p61 = p89;

            {	/* space-adjustments-tracks */
                vname ("space-adjustments-tracks");
                if (print_ISPR18_Space__Adjustments__Tracks (p89, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p90;

        if ((p90 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p90 -> pe_class, p90 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 16))
            p90 = NULLPE;
        if (p90 != NULLPE) {
            p61 = p90;

            {	/* space-adjustment-track */
                vname ("space-adjustment-track");
                if (print_ISPR18_Structured__Name (p90, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p91;

        if ((p91 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p91 -> pe_class, p91 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 17))
            p91 = NULLPE;
        if (p91 != NULLPE) {
            p61 = p91;

            {	/* scores */
                vname ("scores");
                if (print_ISPR18_Scores (p91, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p92;

        if ((p92 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p92 -> pe_class, p92 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 18))
            p92 = NULLPE;
        if (p92 != NULLPE) {
            p61 = p92;

            {	/* variant-scripts */
                vname ("variant-scripts");
                if (print_ISPR18_Variant__Scripts (p92, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p93;

        if ((p93 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p93 -> pe_class, p93 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 19))
            p93 = NULLPE;
        if (p93 != NULLPE) {
            p61 = p93;

            {	/* minimum-line-to-line-spacing */
                vname ("minimum-line-to-line-spacing");
                if (print_ISPR18_Real (p93, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p94;

        if ((p94 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p94 -> pe_class, p94 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 20))
            p94 = NULLPE;
        if (p94 != NULLPE) {
            p61 = p94;

            {	/* minimum-anamorphic-scaling */
                vname ("minimum-anamorphic-scaling");
                if (print_ISPR18_Real (p94, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p95;

        if ((p95 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p95 -> pe_class, p95 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 21))
            p95 = NULLPE;
        if (p95 != NULLPE) {
            p61 = p95;

            {	/* maximum-anamorphic-scaling */
                vname ("maximum-anamorphic-scaling");
                if (print_ISPR18_Real (p95, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p96;

        if ((p96 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p96 -> pe_class, p96 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 22))
            p96 = NULLPE;
        if (p96 != NULLPE) {
            p61 = p96;

            {	/* modal-character-attributes */
                vname ("modal-character-attributes");
                if (print_ISPR18_Modal__Character__Attributes (p96, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p97;

        if ((p97 = (pe != p61 ? next_member (pe, p61) : first_member (pe))) != NULLPE) {
            p61 = p97;

            {	/* non-ISO-attributes */
                register PE p98;

                vname ("non-ISO-attributes");
                if ((p98 = prim2set (p97)) == NULLPE) {
                    advise (NULLCP, "non-ISO-attributes bad set: %s",
                            pe_error (p97 -> pe_errno));
                    return NOTOK;
                }
                p97 = p98;

                vpush ();
                for (p98 = first_member (p97); p98; p98 = next_member (p97, p98)) {
                    if (print_ISPR18_Non__ISO__Attribute (p98, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                        return NOTOK;
                }
                vpop ();
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 24) {
        advise (NULLCP, "Modal-Font-Attributes has too many elements(24): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Space__Adjustments__Tracks (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p99;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Space-Adjustments-Tracks bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Space-Adjustments-Tracks bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p99 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Space-Adjustments-Tracks bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p99;

    vpush ();
    {
        register PE p100;

        if ((p100 = first_member (pe)) != NULLPE
                && PE_ID (p100 -> pe_class, p100 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p100 = NULLPE;
        if (p100 != NULLPE) {
            p99 = p100;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p100, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p101;

        if ((p101 = (pe != p99 ? next_member (pe, p99) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p101 -> pe_class, p101 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 1))
            p101 = NULLPE;
        if (p101 != NULLPE) {
            p99 = p101;

            {	/* tight */
                vname ("tight");
                if (print_ISPR18_Track (p101, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p102;

        if ((p102 = (pe != p99 ? next_member (pe, p99) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p102 -> pe_class, p102 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 2))
            p102 = NULLPE;
        if (p102 != NULLPE) {
            p99 = p102;

            {	/* normal */
                vname ("normal");
                if (print_ISPR18_Track (p102, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p103;

        if ((p103 = (pe != p99 ? next_member (pe, p99) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p103 -> pe_class, p103 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 3))
            p103 = NULLPE;
        if (p103 != NULLPE) {
            p99 = p103;

            {	/* loose */
                vname ("loose");
                if (print_ISPR18_Track (p103, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p104;

        if ((p104 = (pe != p99 ? next_member (pe, p99) : first_member (pe))) != NULLPE) {
            p99 = p104;

            {	/* non-ISO-tracks */
                register PE p105;

                vname ("non-ISO-tracks");
                if ((p105 = prim2set (p104)) == NULLPE) {
                    advise (NULLCP, "non-ISO-tracks bad set: %s",
                            pe_error (p104 -> pe_errno));
                    return NOTOK;
                }
                p104 = p105;

                vpush ();
                for (p105 = first_member (p104); p105; p105 = next_member (p104, p105)) {
                    register PE p106;

                    if (p105 -> pe_class != PE_CLASS_UNIV
                            || p105 -> pe_form != PE_FORM_CONS
                            || p105 -> pe_id != PE_CONS_SEQ) {
                        advise (NULLCP, "member bad class/form/id: %s/%d/0x%x",
                                pe_classlist[p105 -> pe_class], p105 -> pe_form, p105 -> pe_id);
                        return NOTOK;
                    }

                    if ((p106 = prim2seq (p105)) == NULLPE) {
                        advise (NULLCP, "member bad sequence: %s",
                                pe_error (p105 -> pe_errno));
                        return NOTOK;
                    }
                    p105 = p106;

                    vpush ();
                    {
                        register PE p107;

                        if ((p107 = first_member (p105)) != NULLPE) {
                            p106 = p107;

                            {	/* track-name */
                                vname ("track-name");
                                if (print_ISPR18_Structured__Name (p107, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                        else {
                            advise (NULLCP, "member missing track-name element");
                            return NOTOK;
                        }

                    }

                    {
                        register PE p108;

                        if ((p108 = (p105 != p106 ? next_member (p105, p106) : first_member (p105))) != NULLPE) {
                            p106 = p108;

                            {	/* track */
                                vname ("track");
                                if (print_ISPR18_Track (p108, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                        else {
                            advise (NULLCP, "member missing track element");
                            return NOTOK;
                        }

                    }

                    vpop ();

                    if (p105 -> pe_cardinal > 2) {
                        advise (NULLCP, "member has too many elements(2): %d",
                                p105 -> pe_cardinal);
                        return NOTOK;
                    }
                }
                vpop ();
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 5) {
        advise (NULLCP, "Space-Adjustments-Tracks has too many elements(5): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Track (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p109;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Track bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Track bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p109 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Track bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p109;

    vpush ();
    {
        register PE p110;

        if ((p110 = first_member (pe)) != NULLPE
                && PE_ID (p110 -> pe_class, p110 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p110 = NULLPE;
        if (p110 != NULLPE) {
            p109 = p110;

            {	/* slope */
                vname ("slope");
                if (print_ISPR18_Real (p110, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p111;

        if ((p111 = (pe != p109 ? next_member (pe, p109) : first_member (pe))) != NULLPE) {
            p109 = p111;

            {	/* constant */
                vname ("constant");
                if (print_ISPR18_Real (p111, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 2) {
        advise (NULLCP, "Track has too many elements(2): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Scores (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p112;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Scores bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Scores bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p112 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Scores bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p112;

    vpush ();
    {
        register PE p113;

        if ((p113 = first_member (pe)) != NULLPE
                && PE_ID (p113 -> pe_class, p113 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p113 = NULLPE;
        if (p113 != NULLPE) {
            p112 = p113;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p113, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p114;

        if ((p114 = (pe != p112 ? next_member (pe, p112) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p114 -> pe_class, p114 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 1))
            p114 = NULLPE;
        if (p114 != NULLPE) {
            p112 = p114;

            {	/* rightward */
                vname ("rightward");
                if (print_ISPR18_Score (p114, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p115;

        if ((p115 = (pe != p112 ? next_member (pe, p112) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p115 -> pe_class, p115 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 2))
            p115 = NULLPE;
        if (p115 != NULLPE) {
            p112 = p115;

            {	/* leftward */
                vname ("leftward");
                if (print_ISPR18_Score (p115, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p116;

        if ((p116 = (pe != p112 ? next_member (pe, p112) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p116 -> pe_class, p116 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 3))
            p116 = NULLPE;
        if (p116 != NULLPE) {
            p112 = p116;

            {	/* through */
                vname ("through");
                if (print_ISPR18_Score (p116, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p117;

        if ((p117 = (pe != p112 ? next_member (pe, p112) : first_member (pe))) != NULLPE) {
            p112 = p117;

            {	/* non-ISO-scores */
                register PE p118;

                vname ("non-ISO-scores");
                if ((p118 = prim2set (p117)) == NULLPE) {
                    advise (NULLCP, "non-ISO-scores bad set: %s",
                            pe_error (p117 -> pe_errno));
                    return NOTOK;
                }
                p117 = p118;

                vpush ();
                for (p118 = first_member (p117); p118; p118 = next_member (p117, p118)) {
                    register PE p119;

                    if (p118 -> pe_class != PE_CLASS_UNIV
                            || p118 -> pe_form != PE_FORM_CONS
                            || p118 -> pe_id != PE_CONS_SEQ) {
                        advise (NULLCP, "member bad class/form/id: %s/%d/0x%x",
                                pe_classlist[p118 -> pe_class], p118 -> pe_form, p118 -> pe_id);
                        return NOTOK;
                    }

                    if ((p119 = prim2seq (p118)) == NULLPE) {
                        advise (NULLCP, "member bad sequence: %s",
                                pe_error (p118 -> pe_errno));
                        return NOTOK;
                    }
                    p118 = p119;

                    vpush ();
                    {
                        register PE p120;

                        if ((p120 = first_member (p118)) != NULLPE) {
                            p119 = p120;

                            {	/* score-name */
                                vname ("score-name");
                                if (print_ISPR18_Structured__Name (p120, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                        else {
                            advise (NULLCP, "member missing score-name element");
                            return NOTOK;
                        }

                    }

                    {
                        register PE p121;

                        if ((p121 = (p118 != p119 ? next_member (p118, p119) : first_member (p118))) != NULLPE) {
                            p119 = p121;

                            {	/* score */
                                vname ("score");
                                if (print_ISPR18_Score (p121, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                        else {
                            advise (NULLCP, "member missing score element");
                            return NOTOK;
                        }

                    }

                    vpop ();

                    if (p118 -> pe_cardinal > 2) {
                        advise (NULLCP, "member has too many elements(2): %d",
                                p118 -> pe_cardinal);
                        return NOTOK;
                    }
                }
                vpop ();
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 5) {
        advise (NULLCP, "Scores has too many elements(5): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Score (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p122;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Score bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Score bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p122 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Score bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p122;

    vpush ();
    {
        register PE p123;

        if ((p123 = first_member (pe)) != NULLPE
                && PE_ID (p123 -> pe_class, p123 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p123 = NULLPE;
        if (p123 != NULLPE) {
            p122 = p123;

            {	/* position */
                vname ("position");
                if (print_ISPR18_Real (p123, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p124;

        if ((p124 = (pe != p122 ? next_member (pe, p122) : first_member (pe))) != NULLPE) {
            p122 = p124;

            {	/* thickness */
                vname ("thickness");
                if (print_ISPR18_Real (p124, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 2) {
        advise (NULLCP, "Score has too many elements(2): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Variant__Scripts (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p125;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Variant-Scripts bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Variant-Scripts bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p125 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Variant-Scripts bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p125;

    vpush ();
    {
        register PE p126;

        if ((p126 = first_member (pe)) != NULLPE
                && PE_ID (p126 -> pe_class, p126 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p126 = NULLPE;
        if (p126 != NULLPE) {
            p125 = p126;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p126, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p127;

        if ((p127 = (pe != p125 ? next_member (pe, p125) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p127 -> pe_class, p127 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 1))
            p127 = NULLPE;
        if (p127 != NULLPE) {
            p125 = p127;

            {	/* leftward */
                vname ("leftward");
                if (print_ISPR18_Variant__Script (p127, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p128;

        if ((p128 = (pe != p125 ? next_member (pe, p125) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p128 -> pe_class, p128 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 2))
            p128 = NULLPE;
        if (p128 != NULLPE) {
            p125 = p128;

            {	/* rightward */
                vname ("rightward");
                if (print_ISPR18_Variant__Script (p128, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p129;

        if ((p129 = (pe != p125 ? next_member (pe, p125) : first_member (pe))) != NULLPE) {
            p125 = p129;

            {	/* non-ISO-scripts */
                register PE p130;

                vname ("non-ISO-scripts");
                if ((p130 = prim2set (p129)) == NULLPE) {
                    advise (NULLCP, "non-ISO-scripts bad set: %s",
                            pe_error (p129 -> pe_errno));
                    return NOTOK;
                }
                p129 = p130;

                vpush ();
                for (p130 = first_member (p129); p130; p130 = next_member (p129, p130)) {
                    register PE p131;

                    if (p130 -> pe_class != PE_CLASS_UNIV
                            || p130 -> pe_form != PE_FORM_CONS
                            || p130 -> pe_id != PE_CONS_SEQ) {
                        advise (NULLCP, "member bad class/form/id: %s/%d/0x%x",
                                pe_classlist[p130 -> pe_class], p130 -> pe_form, p130 -> pe_id);
                        return NOTOK;
                    }

                    if ((p131 = prim2seq (p130)) == NULLPE) {
                        advise (NULLCP, "member bad sequence: %s",
                                pe_error (p130 -> pe_errno));
                        return NOTOK;
                    }
                    p130 = p131;

                    vpush ();
                    {
                        register PE p132;

                        if ((p132 = first_member (p130)) != NULLPE) {
                            p131 = p132;

                            {	/* script-name */
                                vname ("script-name");
                                if (print_ISPR18_Structured__Name (p132, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                        else {
                            advise (NULLCP, "member missing script-name element");
                            return NOTOK;
                        }

                    }

                    {
                        register PE p133;

                        if ((p133 = (p130 != p131 ? next_member (p130, p131) : first_member (p130))) != NULLPE) {
                            p131 = p133;

                            {	/* script */
                                vname ("script");
                                if (print_ISPR18_Variant__Script (p133, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                        else {
                            advise (NULLCP, "member missing script element");
                            return NOTOK;
                        }

                    }

                    vpop ();

                    if (p130 -> pe_cardinal > 2) {
                        advise (NULLCP, "member has too many elements(2): %d",
                                p130 -> pe_cardinal);
                        return NOTOK;
                    }
                }
                vpop ();
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 4) {
        advise (NULLCP, "Variant-Scripts has too many elements(4): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Variant__Script (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p134;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Variant-Script bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Variant-Script bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p134 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Variant-Script bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p134;

    vpush ();
    {
        register PE p135;

        if ((p135 = first_member (pe)) != NULLPE
                && PE_ID (p135 -> pe_class, p135 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p135 = NULLPE;
        if (p135 != NULLPE) {
            p134 = p135;

            {	/* x-offset */
                vname ("x-offset");
                if (print_ISPR18_Real (p135, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p136;

        if ((p136 = (pe != p134 ? next_member (pe, p134) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p136 -> pe_class, p136 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 1))
            p136 = NULLPE;
        if (p136 != NULLPE) {
            p134 = p136;

            {	/* y-offset */
                vname ("y-offset");
                if (print_ISPR18_Real (p136, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p137;

        if ((p137 = (pe != p134 ? next_member (pe, p134) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p137 -> pe_class, p137 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 2))
            p137 = NULLPE;
        if (p137 != NULLPE) {
            p134 = p137;

            {	/* x-scale */
                vname ("x-scale");
                if (print_ISPR18_Real (p137, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p138;

        if ((p138 = (pe != p134 ? next_member (pe, p134) : first_member (pe))) != NULLPE) {
            p134 = p138;

            {	/* y-scale */
                vname ("y-scale");
                if (print_ISPR18_Real (p138, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 4) {
        advise (NULLCP, "Variant-Script has too many elements(4): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Modal__Character__Attributes (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p139;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Modal-Character-Attributes bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Modal-Character-Attributes bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p139 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Modal-Character-Attributes bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p139;

    vpush ();
    {
        register PE p140;

        if ((p140 = first_member (pe)) != NULLPE
                && PE_ID (p140 -> pe_class, p140 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p140 = NULLPE;
        if (p140 != NULLPE) {
            p139 = p140;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p140, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p141;

        if ((p141 = (pe != p139 ? next_member (pe, p139) : first_member (pe))) != NULLPE) {
            p139 = p141;

            {	/* characters */
                register PE p142;

                vname ("characters");
                if ((p142 = prim2set (p141)) == NULLPE) {
                    advise (NULLCP, "characters bad set: %s",
                            pe_error (p141 -> pe_errno));
                    return NOTOK;
                }
                p141 = p142;

                vpush ();
                for (p142 = first_member (p141); p142; p142 = next_member (p141, p142)) {
                    register PE p143;

                    if (p142 -> pe_class != PE_CLASS_UNIV
                            || p142 -> pe_form != PE_FORM_CONS
                            || p142 -> pe_id != PE_CONS_SEQ) {
                        advise (NULLCP, "member bad class/form/id: %s/%d/0x%x",
                                pe_classlist[p142 -> pe_class], p142 -> pe_form, p142 -> pe_id);
                        return NOTOK;
                    }

                    if ((p143 = prim2seq (p142)) == NULLPE) {
                        advise (NULLCP, "member bad sequence: %s",
                                pe_error (p142 -> pe_errno));
                        return NOTOK;
                    }
                    p142 = p143;

                    vpush ();
                    {
                        register PE p144;

                        if ((p144 = first_member (p142)) != NULLPE
                                && PE_ID (p144 -> pe_class, p144 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 0))
                            p144 = NULLPE;
                        if (p144 != NULLPE) {
                            p143 = p144;

                            {	/* default-name-prefix */
                                vname ("default-name-prefix");
                                if (print_ISPR18_Default__Name__Prefix (p144, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p145;

                        if ((p145 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) != NULLPE) {
                            p143 = p145;

                            {	/* character-name */
                                vname ("character-name");
                                if (print_ISPR18_Structured__Name (p145, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                        else {
                            advise (NULLCP, "member missing character-name element");
                            return NOTOK;
                        }

                    }

                    {
                        register PE p146;

                        if ((p146 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p146 -> pe_class, p146 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 2))
                            p146 = NULLPE;
                        if (p146 != NULLPE) {
                            p143 = p146;

                            {	/* position-x */
                                vname ("position-x");
                                if (print_ISPR18_Real (p146, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p147;

                        if ((p147 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p147 -> pe_class, p147 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 3))
                            p147 = NULLPE;
                        if (p147 != NULLPE) {
                            p143 = p147;

                            {	/* position-y */
                                vname ("position-y");
                                if (print_ISPR18_Real (p147, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p148;

                        if ((p148 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p148 -> pe_class, p148 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 4))
                            p148 = NULLPE;
                        if (p148 != NULLPE) {
                            p143 = p148;

                            {	/* escapement-x */
                                vname ("escapement-x");
                                if (print_ISPR18_Real (p148, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p149;

                        if ((p149 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p149 -> pe_class, p149 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 5))
                            p149 = NULLPE;
                        if (p149 != NULLPE) {
                            p143 = p149;

                            {	/* escapement-y */
                                vname ("escapement-y");
                                if (print_ISPR18_Real (p149, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p150;

                        if ((p150 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p150 -> pe_class, p150 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 6))
                            p150 = NULLPE;
                        if (p150 != NULLPE) {
                            p143 = p150;

                            {	/* forward-extent */
                                vname ("forward-extent");
                                if (print_ISPR18_Real (p150, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p151;

                        if ((p151 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p151 -> pe_class, p151 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 7))
                            p151 = NULLPE;
                        if (p151 != NULLPE) {
                            p143 = p151;

                            {	/* left-extent */
                                vname ("left-extent");
                                if (print_ISPR18_Real (p151, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p152;

                        if ((p152 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p152 -> pe_class, p152 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 8))
                            p152 = NULLPE;
                        if (p152 != NULLPE) {
                            p143 = p152;

                            {	/* backward-extent */
                                vname ("backward-extent");
                                if (print_ISPR18_Real (p152, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p153;

                        if ((p153 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p153 -> pe_class, p153 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 9))
                            p153 = NULLPE;
                        if (p153 != NULLPE) {
                            p143 = p153;

                            {	/* right-extent */
                                vname ("right-extent");
                                if (print_ISPR18_Real (p153, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p154;

                        if ((p154 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p154 -> pe_class, p154 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 10))
                            p154 = NULLPE;
                        if (p154 != NULLPE) {
                            p143 = p154;

                            {	/* char-pair-esc-adjusts */
                                vname ("char-pair-esc-adjusts");
                                if (print_ISPR18_Char__Pair__Esc__Adjusts (p154, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p155;

                        if ((p155 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p155 -> pe_class, p155 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 11))
                            p155 = NULLPE;
                        if (p155 != NULLPE) {
                            p143 = p155;

                            {	/* forward-class-pairwise-class */
                                register int p156;

                                vname ("forward-class-pairwise-class");
                                if ((p156 = prim2num (p155)) == NOTOK
                                        && p155 -> pe_errno != PE_ERR_NONE) {
                                    advise (NULLCP, "forward-class-pairwise-class bad integer: %s",
                                            pe_error (p155 -> pe_errno));
                                    return NOTOK;
                                }
                                vprint ("%d", p156);
                            }
                        }
                    }

                    {
                        register PE p157;

                        if ((p157 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p157 -> pe_class, p157 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 12))
                            p157 = NULLPE;
                        if (p157 != NULLPE) {
                            p143 = p157;

                            {	/* backward-class-pairwise-class */
                                register int p158;

                                vname ("backward-class-pairwise-class");
                                if ((p158 = prim2num (p157)) == NOTOK
                                        && p157 -> pe_errno != PE_ERR_NONE) {
                                    advise (NULLCP, "backward-class-pairwise-class bad integer: %s",
                                            pe_error (p157 -> pe_errno));
                                    return NOTOK;
                                }
                                vprint ("%d", p158);
                            }
                        }
                    }

                    {
                        register PE p159;

                        if ((p159 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p159 -> pe_class, p159 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 13))
                            p159 = NULLPE;
                        if (p159 != NULLPE) {
                            p143 = p159;

                            {	/* forward-sector-space-adjusts */
                                register PE p160;

                                vname ("forward-sector-space-adjusts");
                                if ((p160 = prim2seq (p159)) == NULLPE) {
                                    advise (NULLCP, "forward-sector-space-adjusts bad sequence: %s",
                                            pe_error (p159 -> pe_errno));
                                    return NOTOK;
                                }
                                p159 = p160;

                                vpush ();
                                for (p160 = first_member (p159); p160; p160 = next_member (p159, p160)) {
                                    if (print_ISPR18_Real (p160, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                                        return NOTOK;
                                }
                                vpop ();
                            }
                        }
                    }

                    {
                        register PE p161;

                        if ((p161 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p161 -> pe_class, p161 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 14))
                            p161 = NULLPE;
                        if (p161 != NULLPE) {
                            p143 = p161;

                            {	/* backward-sector-space-adjusts */
                                register PE p162;

                                vname ("backward-sector-space-adjusts");
                                if ((p162 = prim2seq (p161)) == NULLPE) {
                                    advise (NULLCP, "backward-sector-space-adjusts bad sequence: %s",
                                            pe_error (p161 -> pe_errno));
                                    return NOTOK;
                                }
                                p161 = p162;

                                vpush ();
                                for (p162 = first_member (p161); p162; p162 = next_member (p161, p162)) {
                                    if (print_ISPR18_Real (p162, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                                        return NOTOK;
                                }
                                vpop ();
                            }
                        }
                    }

                    {
                        register PE p163;

                        if ((p163 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p163 -> pe_class, p163 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 15))
                            p163 = NULLPE;
                        if (p163 != NULLPE) {
                            p143 = p163;

                            {	/* amplification */
                                register int p164;

                                vname ("amplification");
                                if ((p164 = prim2num (p163)) == NOTOK
                                        && p163 -> pe_errno != PE_ERR_NONE) {
                                    advise (NULLCP, "amplification bad integer: %s",
                                            pe_error (p163 -> pe_errno));
                                    return NOTOK;
                                }
                                vprint ("%d", p164);
                            }
                        }
                    }

                    {
                        register PE p165;

                        if ((p165 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p165 -> pe_class, p165 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 16))
                            p165 = NULLPE;
                        if (p165 != NULLPE) {
                            p143 = p165;

                            {	/* correction */
                                register int p166;

                                vname ("correction");
                                if ((p166 = prim2num (p165)) == NOTOK
                                        && p165 -> pe_errno != PE_ERR_NONE) {
                                    advise (NULLCP, "correction bad integer: %s",
                                            pe_error (p165 -> pe_errno));
                                    return NOTOK;
                                }
                                vprint ("%d", p166);
                            }
                        }
                    }

                    {
                        register PE p167;

                        if ((p167 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p167 -> pe_class, p167 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 17))
                            p167 = NULLPE;
                        if (p167 != NULLPE) {
                            p143 = p167;

                            {	/* tracked-space-adjust-indicator */
                                register int p168;

                                vname ("tracked-space-adjust-indicator");
                                if ((p168 = prim2num (p167)) == NOTOK
                                        && p167 -> pe_errno != PE_ERR_NONE) {
                                    advise (NULLCP, "tracked-space-adjust-indicator bad integer: %s",
                                            pe_error (p167 -> pe_errno));
                                    return NOTOK;
                                }
                                vprint ("%d", p168);
                            }
                        }
                    }

                    {
                        register PE p169;

                        if ((p169 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) 
                                    != NULLPE
                                && PE_ID (p169 -> pe_class, p169 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 18))
                            p169 = NULLPE;
                        if (p169 != NULLPE) {
                            p143 = p169;

                            {	/* frequency-of-use */
                                vname ("frequency-of-use");
                                if (print_ISPR18_Real (p169, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p170;

                        if ((p170 = (p142 != p143 ? next_member (p142, p143) : first_member (p142))) != NULLPE) {
                            p143 = p170;

                            {	/* non-ISO-attributes */
                                register PE p171;

                                vname ("non-ISO-attributes");
                                if ((p171 = prim2set (p170)) == NULLPE) {
                                    advise (NULLCP, "non-ISO-attributes bad set: %s",
                                            pe_error (p170 -> pe_errno));
                                    return NOTOK;
                                }
                                p170 = p171;

                                vpush ();
                                for (p171 = first_member (p170); p171; p171 = next_member (p170, p171)) {
                                    if (print_ISPR18_Non__ISO__Attribute (p171, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                                        return NOTOK;
                                }
                                vpop ();
                            }
                        }
                    }

                    vpop ();

                    if (p142 -> pe_cardinal > 20) {
                        advise (NULLCP, "member has too many elements(20): %d",
                                p142 -> pe_cardinal);
                        return NOTOK;
                    }
                }
                vpop ();
            }
        }
        else {
            advise (NULLCP, "Modal-Character-Attributes missing characters element");
            return NOTOK;
        }

    }

    vpop ();

    if (pe -> pe_cardinal > 2) {
        advise (NULLCP, "Modal-Character-Attributes has too many elements(2): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Char__Pair__Esc__Adjusts (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p172;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Char-Pair-Esc-Adjusts bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Char-Pair-Esc-Adjusts bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p172 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Char-Pair-Esc-Adjusts bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p172;

    vpush ();
    {
        register PE p173;

        if ((p173 = first_member (pe)) != NULLPE
                && PE_ID (p173 -> pe_class, p173 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p173 = NULLPE;
        if (p173 != NULLPE) {
            p172 = p173;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p173, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p174;

        if ((p174 = (pe != p172 ? next_member (pe, p172) : first_member (pe))) != NULLPE) {
            p172 = p174;

            {	/* successor-characters */
                register PE p175;

                vname ("successor-characters");
                if ((p175 = prim2set (p174)) == NULLPE) {
                    advise (NULLCP, "successor-characters bad set: %s",
                            pe_error (p174 -> pe_errno));
                    return NOTOK;
                }
                p174 = p175;

                vpush ();
                for (p175 = first_member (p174); p175; p175 = next_member (p174, p175)) {
                    register PE p176;

                    if (p175 -> pe_class != PE_CLASS_UNIV
                            || p175 -> pe_form != PE_FORM_CONS
                            || p175 -> pe_id != PE_CONS_SEQ) {
                        advise (NULLCP, "member bad class/form/id: %s/%d/0x%x",
                                pe_classlist[p175 -> pe_class], p175 -> pe_form, p175 -> pe_id);
                        return NOTOK;
                    }

                    if ((p176 = prim2seq (p175)) == NULLPE) {
                        advise (NULLCP, "member bad sequence: %s",
                                pe_error (p175 -> pe_errno));
                        return NOTOK;
                    }
                    p175 = p176;

                    vpush ();
                    {
                        register PE p177;

                        if ((p177 = first_member (p175)) != NULLPE) {
                            p176 = p177;

                            {	/* successor-character-name */
                                vname ("successor-character-name");
                                if (print_ISPR18_Structured__Name (p177, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                        else {
                            advise (NULLCP, "member missing successor-character-name element");
                            return NOTOK;
                        }

                    }

                    {
                        register PE p178;

                        if ((p178 = (p175 != p176 ? next_member (p175, p176) : first_member (p175))) 
                                    != NULLPE
                                && PE_ID (p178 -> pe_class, p178 -> pe_id)
                                        != PE_ID (PE_CLASS_CONT, 1))
                            p178 = NULLPE;
                        if (p178 != NULLPE) {
                            p176 = p178;

                            {	/* adjustment-x */
                                vname ("adjustment-x");
                                if (print_ISPR18_Real (p178, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    {
                        register PE p179;

                        if ((p179 = (p175 != p176 ? next_member (p175, p176) : first_member (p175))) != NULLPE) {
                            p176 = p179;

                            {	/* adjustment-y */
                                vname ("adjustment-y");
                                if (print_ISPR18_Real (p179, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                    }

                    vpop ();

                    if (p175 -> pe_cardinal > 3) {
                        advise (NULLCP, "member has too many elements(3): %d",
                                p175 -> pe_cardinal);
                        return NOTOK;
                    }
                }
                vpop ();
            }
        }
        else {
            advise (NULLCP, "Char-Pair-Esc-Adjusts missing successor-characters element");
            return NOTOK;
        }

    }

    vpop ();

    if (pe -> pe_cardinal > 2) {
        advise (NULLCP, "Char-Pair-Esc-Adjusts has too many elements(2): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Character__Shape__Technologies (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p180;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Character-Shape-Technologies bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Character-Shape-Technologies bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p180 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Character-Shape-Technologies bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p180;

    vpush ();
    {
        register PE p181;

        if ((p181 = first_member (pe)) != NULLPE
                && PE_ID (p181 -> pe_class, p181 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p181 = NULLPE;
        if (p181 != NULLPE) {
            p180 = p181;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p181, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p182;

        if ((p182 = (pe != p180 ? next_member (pe, p180) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p182 -> pe_class, p182 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 1))
            p182 = NULLPE;
        if (p182 != NULLPE) {
            p180 = p182;

            {	/* bitmap */
                vname ("bitmap");
                if (print_ISPR18_Technology__Attributes (p182, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p183;

        if ((p183 = (pe != p180 ? next_member (pe, p180) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p183 -> pe_class, p183 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 2))
            p183 = NULLPE;
        if (p183 != NULLPE) {
            p180 = p183;

            {	/* straight-line-outline */
                vname ("straight-line-outline");
                if (print_ISPR18_Technology__Attributes (p183, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p184;

        if ((p184 = (pe != p180 ? next_member (pe, p180) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p184 -> pe_class, p184 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 3))
            p184 = NULLPE;
        if (p184 != NULLPE) {
            p180 = p184;

            {	/* circular-outline */
                vname ("circular-outline");
                if (print_ISPR18_Technology__Attributes (p184, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p185;

        if ((p185 = (pe != p180 ? next_member (pe, p180) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p185 -> pe_class, p185 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 4))
            p185 = NULLPE;
        if (p185 != NULLPE) {
            p180 = p185;

            {	/* conic-outline */
                vname ("conic-outline");
                if (print_ISPR18_Technology__Attributes (p185, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p186;

        if ((p186 = (pe != p180 ? next_member (pe, p180) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p186 -> pe_class, p186 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 5))
            p186 = NULLPE;
        if (p186 != NULLPE) {
            p180 = p186;

            {	/* bezier-outline */
                vname ("bezier-outline");
                if (print_ISPR18_Technology__Attributes (p186, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p187;

        if ((p187 = (pe != p180 ? next_member (pe, p180) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p187 -> pe_class, p187 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 6))
            p187 = NULLPE;
        if (p187 != NULLPE) {
            p180 = p187;

            {	/* mixed */
                vname ("mixed");
                if (print_ISPR18_Technology__Attributes (p187, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p188;

        if ((p188 = (pe != p180 ? next_member (pe, p180) : first_member (pe))) != NULLPE) {
            p180 = p188;

            {	/* non-ISO-technologies */
                register PE p189;

                vname ("non-ISO-technologies");
                if ((p189 = prim2set (p188)) == NULLPE) {
                    advise (NULLCP, "non-ISO-technologies bad set: %s",
                            pe_error (p188 -> pe_errno));
                    return NOTOK;
                }
                p188 = p189;

                vpush ();
                for (p189 = first_member (p188); p189; p189 = next_member (p188, p189)) {
                    register PE p190;

                    if (p189 -> pe_class != PE_CLASS_UNIV
                            || p189 -> pe_form != PE_FORM_CONS
                            || p189 -> pe_id != PE_CONS_SEQ) {
                        advise (NULLCP, "member bad class/form/id: %s/%d/0x%x",
                                pe_classlist[p189 -> pe_class], p189 -> pe_form, p189 -> pe_id);
                        return NOTOK;
                    }

                    if ((p190 = prim2seq (p189)) == NULLPE) {
                        advise (NULLCP, "member bad sequence: %s",
                                pe_error (p189 -> pe_errno));
                        return NOTOK;
                    }
                    p189 = p190;

                    vpush ();
                    {
                        register PE p191;

                        if ((p191 = first_member (p189)) != NULLPE) {
                            p190 = p191;

                            {	/* technology-name */
                                vname ("technology-name");
                                if (print_ISPR18_Structured__Name (p191, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                        else {
                            advise (NULLCP, "member missing technology-name element");
                            return NOTOK;
                        }

                    }

                    {
                        register PE p192;

                        if ((p192 = (p189 != p190 ? next_member (p189, p190) : first_member (p189))) != NULLPE) {
                            p190 = p192;

                            {	/* technology-attributes */
                                vname ("technology-attributes");
                                if (print_ISPR18_Technology__Attributes (p192, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                                    return NOTOK;
                            }
                        }
                        else {
                            advise (NULLCP, "member missing technology-attributes element");
                            return NOTOK;
                        }

                    }

                    vpop ();

                    if (p189 -> pe_cardinal > 2) {
                        advise (NULLCP, "member has too many elements(2): %d",
                                p189 -> pe_cardinal);
                        return NOTOK;
                    }
                }
                vpop ();
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 8) {
        advise (NULLCP, "Character-Shape-Technologies has too many elements(8): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Technology__Attributes (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p193;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Technology-Attributes bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Technology-Attributes bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p193 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Technology-Attributes bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p193;

    vpush ();
    {
        register PE p194;

        if ((p194 = first_member (pe)) != NULLPE
                && PE_ID (p194 -> pe_class, p194 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 0))
            p194 = NULLPE;
        if (p194 != NULLPE) {
            p193 = p194;

            {	/* default-name-prefix */
                vname ("default-name-prefix");
                if (print_ISPR18_Default__Name__Prefix (p194, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p195;

        if ((p195 = (pe != p193 ? next_member (pe, p193) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p195 -> pe_class, p195 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 1))
            p195 = NULLPE;
        if (p195 != NULLPE) {
            p193 = p195;

            {	/* fast-scan-direction */
                vname ("fast-scan-direction");
                if (print_ISPR18_Real (p195, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p196;

        if ((p196 = (pe != p193 ? next_member (pe, p193) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p196 -> pe_class, p196 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 2))
            p196 = NULLPE;
        if (p196 != NULLPE) {
            p193 = p196;

            {	/* slow-scan-direction */
                register int p197;

                vname ("slow-scan-direction");
                if ((p197 = prim2num (p196)) == NOTOK
                        && p196 -> pe_errno != PE_ERR_NONE) {
                    advise (NULLCP, "slow-scan-direction bad integer: %s",
                            pe_error (p196 -> pe_errno));
                    return NOTOK;
                }
                vprint ("%d", p197);
            }
        }
    }

    {
        register PE p198;

        if ((p198 = (pe != p193 ? next_member (pe, p193) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p198 -> pe_class, p198 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 3))
            p198 = NULLPE;
        if (p198 != NULLPE) {
            p193 = p198;

            {	/* fast-scan-resolution */
                vname ("fast-scan-resolution");
                if (print_ISPR18_Real (p198, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p199;

        if ((p199 = (pe != p193 ? next_member (pe, p193) : first_member (pe))) 
                    != NULLPE
                && PE_ID (p199 -> pe_class, p199 -> pe_id)
                        != PE_ID (PE_CLASS_CONT, 4))
            p199 = NULLPE;
        if (p199 != NULLPE) {
            p193 = p199;

            {	/* slow-scan-resolution */
                vname ("slow-scan-resolution");
                if (print_ISPR18_Real (p199, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
    }

    {
        register PE p200;

        if ((p200 = (pe != p193 ? next_member (pe, p193) : first_member (pe))) != NULLPE) {
            p193 = p200;

            {	/* non-ISO-Attributes */
                register PE p201;

                vname ("non-ISO-Attributes");
                if ((p201 = prim2set (p200)) == NULLPE) {
                    advise (NULLCP, "non-ISO-Attributes bad set: %s",
                            pe_error (p200 -> pe_errno));
                    return NOTOK;
                }
                p200 = p201;

                vpush ();
                for (p201 = first_member (p200); p201; p201 = next_member (p200, p201)) {
                    if (print_ISPR18_Non__ISO__Attribute (p201, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                        return NOTOK;
                }
                vpop ();
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 6) {
        advise (NULLCP, "Technology-Attributes has too many elements(6): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Non__ISO__Attribute (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p202;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Non-ISO-Attribute bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Non-ISO-Attribute bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p202 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Non-ISO-Attribute bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p202;

    vpush ();
    {
        register PE p203;

        if ((p203 = first_member (pe)) != NULLPE) {
            p202 = p203;

            {	/* attribute-name */
                vname ("attribute-name");
                if (print_ISPR18_Structured__Name (p203, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
        }
        else {
            advise (NULLCP, "Non-ISO-Attribute missing attribute-name element");
            return NOTOK;
        }

    }

    {
        register PE p204;

        if ((p204 = (pe != p202 ? next_member (pe, p202) : first_member (pe))) != NULLPE) {
            p202 = p204;

            {	/* attribute-value */
                vname ("attribute-value");
                vunknown (p204);
            }
        }
        else {
            advise (NULLCP, "Non-ISO-Attribute missing attribute-value element");
            return NOTOK;
        }

    }

    vpop ();

    if (pe -> pe_cardinal > 2) {
        advise (NULLCP, "Non-ISO-Attribute has too many elements(2): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Structured__Name (pe, explicit, len, buffer, parm)
PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    vpush ();
    switch (PE_ID (pe -> pe_class, pe -> pe_id)) {
        case PE_ID (PE_CLASS_CONT, 0):	/* complete-structured-name */
            {
                register PE p205;

                vname ("complete-structured-name");
                if ((p205 = prim2seq (pe)) == NULLPE) {
                    advise (NULLCP, "complete-structured-name bad sequence: %s",
                            pe_error (pe -> pe_errno));
                    return NOTOK;
                }
                pe = p205;

                vpush ();
                for (p205 = first_member (pe); p205; p205 = next_member (pe, p205)) {
                    if (print_ISPR18_Identifier (p205, 1, NULLIP, NULLVP, NullParm) == NOTOK)
                        return NOTOK;
                }
                vpop ();
            }
            break;
        case PE_ID (PE_CLASS_CONT, 1):	/* abbreviated-structured-name */
            {
                vname ("abbreviated-structured-name");
                if (print_ISPR18_Identifier (pe, 0, NULLIP, NULLVP, NullParm) == NOTOK)
                    return NOTOK;
            }
            break;

        default:
            advise (NULLCP, "Structured-Name has unknown choice: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
    }
    vpop ();

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Default__Name__Prefix (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p206;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Default-Name-Prefix bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Default-Name-Prefix bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p206 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Default-Name-Prefix bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p206;

    vpush ();
    for (p206 = first_member (pe); p206; p206 = next_member (pe, p206)) {
        if (print_ISPR18_Identifier (p206, 1, NULLIP, NULLVP, NullParm) == NOTOK)
            return NOTOK;
    }
    vpop ();

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Identifier (pe, explicit, len, buffer, parm)
PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    vpush ();
    switch (PE_ID (pe -> pe_class, pe -> pe_id)) {
        case PE_ID (PE_CLASS_CONT, 0):	/* integer */
            {
                register int p207;

                vname ("integer");
                if ((p207 = prim2num (pe)) == NOTOK
                        && pe -> pe_errno != PE_ERR_NONE) {
                    advise (NULLCP, "integer bad integer: %s",
                            pe_error (pe -> pe_errno));
                    return NOTOK;
                }
                vprint ("%d", p207);
            }
            break;
        case PE_ID (PE_CLASS_CONT, 1):	/* character-sequence */
            {
                vname ("character-sequence");
                vstring (pe);
            }
            break;

        default:
            advise (NULLCP, "Identifier has unknown choice: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
    }
    vpop ();

    return OK;
}

/* ARGSUSED */

int	print_ISPR18_Real (pe, explicit, len, buffer, parm)
register PE	pe;
int	explicit;
int    *len;
char  **buffer;
PEPYPARM parm;
{
    register PE p208;

    if (explicit) {
        if (pe -> pe_class != PE_CLASS_UNIV
                || pe -> pe_form != PE_FORM_CONS
                || pe -> pe_id != PE_CONS_SEQ) {
            advise (NULLCP, "Real bad class/form/id: %s/%d/0x%x",
                    pe_classlist[pe -> pe_class], pe -> pe_form, pe -> pe_id);
            return NOTOK;
        }
    }
    else
        if (pe -> pe_form != PE_FORM_CONS) {
            advise (NULLCP, "Real bad form: %d", pe -> pe_form);
            return NOTOK;
        }

    if ((p208 = prim2seq (pe)) == NULLPE) {
        advise (NULLCP, "Real bad sequence: %s",
                pe_error (pe -> pe_errno));
        return NOTOK;
    }
    pe = p208;

    vpush ();
    {
        register PE p209;

        if ((p209 = first_member (pe)) != NULLPE) {
            p208 = p209;

            {	/* numerator */
                register int p210;

                vname ("numerator");
                if ((p210 = prim2num (p209)) == NOTOK
                        && p209 -> pe_errno != PE_ERR_NONE) {
                    advise (NULLCP, "numerator bad integer: %s",
                            pe_error (p209 -> pe_errno));
                    return NOTOK;
                }
                vprint ("%d", p210);
            }
        }
        else {
            advise (NULLCP, "Real missing numerator element");
            return NOTOK;
        }

    }

    {
        register PE p211;

        if ((p211 = (pe != p208 ? next_member (pe, p208) : first_member (pe))) != NULLPE) {
            p208 = p211;

            {	/* denominator */
                register int p212;

                vname ("denominator");
                if ((p212 = prim2num (p211)) == NOTOK
                        && p211 -> pe_errno != PE_ERR_NONE) {
                    advise (NULLCP, "denominator bad integer: %s",
                            pe_error (p211 -> pe_errno));
                    return NOTOK;
                }
                vprint ("%d", p212);
            }
        }
    }

    vpop ();

    if (pe -> pe_cardinal > 2) {
        advise (NULLCP, "Real has too many elements(2): %d",
                pe -> pe_cardinal);
        return NOTOK;
    }

    return OK;
}
