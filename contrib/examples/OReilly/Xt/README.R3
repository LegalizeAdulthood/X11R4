This distribution builds under R3 and R4.  The only exceptions
are the following files, which build only under R4:

ch12/xmenu3		(Uses R4 MenuButton widget)
ch12/xmenu7		(Uses all R4 Menu widgets)

This distribution has been tested on BSD and System V machines.
