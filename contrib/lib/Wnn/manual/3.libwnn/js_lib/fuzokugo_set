JS_FUZOKUGO_SET( )

名称

	js_fuzokugo_set		付属語ファイルを指定する。

形式

	#include "jslib.h"

	int
	js_fuzokugo_set(env,fid)
	struct wnn_env *env;		/* 環境 */
	int     fid;			/* 付属語ファイルのID */

機能

	環境の付属語を fid で指定されたものに切り替える。
	指定したファイルが、付属語ファイル以外の場合は、何もしない。

リターンバリュー

	正常終了時には、0 を返す。
	異常終了時には、-1 を返す。

