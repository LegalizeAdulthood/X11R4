JS_KILL( )

名称

	js_kill		サーバを終了させる。

形式

	#include "jslib.h"

	int
	js_kill(server)
	WNN_JSERVER_ID *server;	

機能

	サーバ server にコネクトしているクライアントが他に存在しない場合、
	0 を返し server を終了させる。他にクライアントが存在している場合には、
	自分を除いたクライアント数が返される。

リターンバリュー
	自分を除いたクライアント数が返される。

