JS_FILE_DISCARD( )

名称

	js_file_discard		ファイルをサーバ上からなくす。

形式

	#include "jslib.h"

	int
	js_file_discard(env,fid)
	struct wnn_env *env;	/* 環境 */
	int     fid;		/* ファイルID */

機能

	ファイルID fid のファイルをサーバ上からなくす。
	ただし、参照が残っている場合はこのかぎりではない。
	ファイルに辞書を指定した場合は、頻度ファイルも同時になくす。
	ファイルに頻度ファイルを指定した場合は、頻度ファイルをなくす。
        ファイルに付属語ファイルを指定した場合は、何もしない。/????/

リターンバリュー

	正常終了時には、0 を返す。
	異常終了時には、-1 を返す。

